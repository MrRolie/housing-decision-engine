{
 "context": "Repo: housing-decision-engine, worktree /home/mm-mike/ai_system/projects/housing-decision-engine.demoflow-t1, branch feat/demoflow-tranche1 (demoflow Tranche-1 build; seat-run).\n\nGOVERNING FILES (read before implementing; they rule):\n- docs/specs/2026-07-21-demoflow-demographic-scenario-module-design.md — the LOCKED spec. NO-TOUCH. It is the GOVERNING description of this system. §7 carries the ED/OwnerStock equations, the rankings contract, the identity envelope, the closed flags/reason enums and the \"no open string anywhere\" rule; §6 carries rulings P–T and amendments #7–#15.\n- docs/plans/2026-07-21-demoflow-tranche1.md — HISTORICAL, and read for INTENT only. Since it was written the spec has taken NINE amendments and rulings A through U, several of which REFUTED plan premises outright. **Where the plan and the spec disagree, the SPEC wins.** Plan test bodies never got the spec's review depth — treat plan code as reviewable, not gospel, and report every divergence you act on.\n- demoflow/ is its own uv project. Canonical gate: `./scripts/test-all.sh` from the worktree root. Green at dispatch, DERIVED at authoring time from the seat's OWN gate run: tree CLEAN at HEAD `5926f13`, hde **191** + demoflow **851**.\n\nTHIS RUN IS PLAN TASK 29, CUT INTO THREE SEQUENTIAL TASKS by the seat: (1) artifacts.py, (2) pipeline.py, (3) cli.py. They are dependency-ordered — task 2 imports task 1, task 3 imports task 2. The seat cut it because the 418-line single body is the arc's most carry-laden and a single dropped carry drops a ruling.\n\n**THE PLAN BODY FOR THIS TASK IS KNOWN-DEFECTIVE IN TWELVE PLACES, audited by the seat before dispatch.** Most seriously, its `_tripwire_results` re-introduces the exact hardcoded false-green that Task 28 and run 29 were built to eliminate. Every defect is named in the task mandates as a binding carry. Where a carry conflicts with the plan bytes quoted beside it, THE CARRY WINS.\n\nFOOTPRINT: demoflow/** only. Nothing touches docs/specs/**, docs/plans/**. `loaders/hors_aligned.py` is READ-and-IMPORT only — task 2 must CONSUME it; do not edit it.",
 "worktree": "/home/mm-mike/ai_system/projects/housing-decision-engine.demoflow-t1",
 "branch": "feat/demoflow-tranche1",
 "model_impl": "opus",
 "model_review": "opus",
 "tasks": [
  {
   "title": "Task 29a — artifacts.py (identity envelope, strict writer, closed exclusion schema)",
   "instructions": "### Task 29a: `artifacts.py` — identity envelope, strict writer, closed exclusion schema\n\nThis is the FIRST of three tasks the seat cut out of plan Task 29. Build ONLY `artifacts.py` and its\ntests here; `pipeline.py` and `cli.py` are tasks 2 and 3 of this run and must not be written yet.\n\nPlan Task 29's header, for context (the plan is HISTORICAL — see the governing-files note):\n\n### Task 29: Pipeline orchestrator + CLI (`demoflow run`, `demoflow tripwires`)\n\n**Files:**\n- Create: `demoflow/src/demoflow/output/artifacts.py`\n- Create: `demoflow/src/demoflow/pipeline.py`\n- Create: `demoflow/src/demoflow/cli.py`\n- Test: `demoflow/tests/test_pipeline.py`\n\nRankings are a byproduct of `run` (single vintage, spec §7b). Wires the ROUNDS 7–9 contracts too:\nthe **identity envelope** {schema_version, data_vintage (incl source_hashes), assumptions_hash} above\nthe rows; the **projected-years-only** ranking domain (Statut=proj, contiguous through 2051); the\n**central-value RUN CONTRACT** (q_live 0.085, φ 0.9/0.725, L=2, ratio center) + a per-geography\n**rank_stable** robustness sweep; **P_resident ≥ 0** per cell before any consumer; the **general\nno-open-string** tree-walk (source_hashes keys registry-bound, hashes 64-hex, timestamps ISO-8601).\nAll JSON `allow_nan=False` + finiteness asserted pre-write.\n\n\n**Plan bytes for this file, extracted mechanically:**\n\n`demoflow/src/demoflow/output/artifacts.py`:\n```python\n\"\"\"Golden-artifact JSON writers (spec §4/§7/§9). Identity envelope {schema_version,\ndata_vintage (incl source_hashes), assumptions_hash} above the rows; allow_nan=False +\nfinite pre-write; row allowlists + a general no-open-string tree-walk (codex r7-F6/r9-F3).\"\"\"\nimport json\nimport math\nimport re\nfrom pathlib import Path\n\nfrom demoflow.output.rankings import GeoRanking, assert_rankings_row_valid, ranking_row\nfrom demoflow.output.tripwires import TripwireResult, assert_tripwire_record_valid, tripwire_record\n\n_HEX64 = re.compile(r\"^[0-9a-f]{64}$\")\n_ISO_DATE = re.compile(r\"^\\d{4}-\\d{2}-\\d{2}([T ].*)?$\")\n\n\ndef _assert_finite(obj) -> None:\n    if isinstance(obj, float) and not math.isfinite(obj):\n        raise ValueError(f\"non-finite value in artifact: {obj}\")\n    if isinstance(obj, dict):\n        for v in obj.values():\n            _assert_finite(v)\n    elif isinstance(obj, (list, tuple)):\n        for v in obj:\n            _assert_finite(v)\n\n\ndef assert_no_open_strings(doc: dict, allowed_source_keys: frozenset) -> None:\n    \"\"\"Every string position is registry/enum-bound or format-validated (codex r9-F3). Closes the\n    source_hashes-KEY side-channel; validates sha256 (64-hex) + extracted_at (ISO-8601). Row-level\n    enum/allowlist binding is enforced by assert_rankings_row_valid / assert_tripwire_record_valid.\"\"\"\n    sh = doc.get(\"data_vintage\", {}).get(\"source_hashes\", {})\n    for key, val in sh.items():\n        if key not in allowed_source_keys:\n            raise ValueError(f\"source_hashes key {key!r} not in the code-owned source registry\")\n        if not _HEX64.match(str(val.get(\"sha256\", \"\"))):\n            raise ValueError(f\"source_hashes[{key}].sha256 is not 64-hex\")\n        if not _ISO_DATE.match(str(val.get(\"extracted_at\", \"\"))):\n            raise ValueError(f\"source_hashes[{key}].extracted_at is not ISO-8601\")\n\n\ndef write_json_strict(path: Path, obj: dict) -> None:\n    _assert_finite(obj)                       # finite pre-write (codex r4-F3)\n    with open(path, \"w\") as fh:\n        json.dump(obj, fh, allow_nan=False, indent=2, sort_keys=True)\n        fh.write(\"\\n\")\n\n\ndef _envelope(schema: str, vintage: dict, assumptions_hash: str) -> dict:\n    return {\"schema\": schema, \"schema_version\": \"1\", \"data_vintage\": vintage,\n            \"assumptions_hash\": assumptions_hash}\n\n\n# Run-level exclusion (codex r10): a geography whose demand-side input is unresolvable (HORS_RMR\n# fallback iii) is EXCLUDED FROM RANKINGS ENTIRELY — no ED row — and named in a typed exclusion\n# record. `unresolved_input` is a CLOSED enum, never free text.\n_UNRESOLVED_INPUTS = frozenset({\"immigrant_component_flows\"})\n\n\ndef rankings_document(rankings, vintage, assumptions_hash, allowed_source_keys, exclusions=()) -> dict:\n    rows = [ranking_row(r) for r in rankings]\n    for row in rows:\n        assert_rankings_row_valid(row)        # closed allowlist + flags enum + typed rank_stable\n    for exc in exclusions:\n        if set(exc) != {\"geography\", \"unresolved_input\"} or exc[\"unresolved_input\"] not in _UNRESOLVED_INPUTS:\n            raise ValueError(f\"exclusion record not in the closed schema {{geography, unresolved_input∈\"\n                             f\"{sorted(_UNRESOLVED_INPUTS)}}}: {exc}\")\n    doc = {**_envelope(\"demoflow.rankings.v1\", vintage, assumptions_hash),\n           \"rankings\": rows, \"exclusions\": list(exclusions)}\n    assert_no_open_strings(doc, allowed_source_keys)\n    return doc\n\n\ndef tripwire_document(results, vintage, assumptions_hash, allowed_source_keys) -> dict:\n    recs = [tripwire_record(t) for t in results]\n    for rec in recs:\n        assert_tripwire_record_valid(rec)     # closed allowlist + reason enum + registry-bound source\n    doc = {**_envelope(\"demoflow.tripwire_baseline.v1\", vintage, assumptions_hash), \"indicators\": recs}\n    assert_no_open_strings(doc, allowed_source_keys)\n    return doc\n```\n\n\nThe relevant plan test cases are `test_artifacts_reject_nan`, `test_no_open_string_rejects_smuggled_source_hashes_key`\nand `test_hors_rmr_fallback_iii_excluded_from_rankings_with_typed_record`, inside this block:\n\n- [ ] **Step 1: Write the failing test**\n\n`demoflow/tests/test_pipeline.py`:\n```python\nimport json\nimport math\n\nimport pytest\n\nfrom demoflow.pipeline import run_pipeline, HORIZON_YEARS\nfrom demoflow.output.tripwires import REQUIRED_INDICATORS\n\n\ndef test_run_pipeline_emits_two_json_artifacts_with_identity_envelope(tmp_path):\n    run_pipeline(out_dir=tmp_path, now_year=2026)\n    ranks = json.loads((tmp_path / \"rankings.json\").read_text())\n    assert ranks[\"schema\"] == \"demoflow.rankings.v1\"\n    assert \"schema_version\" in ranks and \"assumptions_hash\" in ranks        # identity envelope (r7-F6)\n    assert \"source_hashes\" in ranks[\"data_vintage\"]                         # codex r3-F6\n    r0 = ranks[\"rankings\"][0]\n    assert isinstance(r0[\"geography\"], str) and r0[\"rank\"] == 1\n    assert set(r0) == {\"geography\", \"mean_ed_reference\", \"mean_ed_low\", \"mean_ed_high\",\n                       \"rank\", \"rank_stable\", \"flags\"}\n    assert isinstance(r0[\"rank_stable\"], bool)                              # typed sweep verdict\n\n    trip = json.loads((tmp_path / \"tripwire_baseline.json\").read_text())\n    assert trip[\"schema\"] == \"demoflow.tripwire_baseline.v1\"\n    assert trip[\"assumptions_hash\"] == ranks[\"assumptions_hash\"]            # same identity envelope\n    inds = {t[\"indicator\"] for t in trip[\"indicators\"]}\n    assert REQUIRED_INDICATORS <= inds\n    for t in trip[\"indicators\"]:\n        assert {\"indicator\", \"current_value\", \"source\", \"as_of\", \"band_low\", \"band_high\", \"status\"} <= set(t)\n\n\ndef test_horizons_are_the_declared_set():\n    assert HORIZON_YEARS == [2030, 2035, 2040, 2045, 2050]\n\n\ndef test_artifacts_reject_nan(tmp_path):\n    from demoflow.output.artifacts import write_json_strict\n    with pytest.raises(ValueError):\n        write_json_strict(tmp_path / \"x.json\", {\"schema\": \"t\", \"v\": math.nan})\n\n\ndef test_no_open_string_rejects_smuggled_source_hashes_key():\n    from demoflow.output.artifacts import assert_no_open_strings\n    doc = {\"data_vintage\": {\"source_hashes\": {\n        \"crash_probability=0.35\": {\"sha256\": \"0\" * 64, \"extracted_at\": \"2026-07-21\"}}}}\n    with pytest.raises(ValueError, match=\"source_hashes key\"):\n        assert_no_open_strings(doc, frozenset({\"ownership_by_geo_age.json\"}))\n\n\ndef test_two_vintage_mixing_refused():\n    from demoflow.output.rankings import refuse_cross_vintage\n    from demoflow.errors import CalibrationError\n    with pytest.raises(CalibrationError):\n        refuse_cross_vintage({\"hashA\", \"hashB\"})     # identity envelope drives the refusal\n\n\ndef test_hors_rmr_fallback_iii_excluded_from_rankings_with_typed_record():\n    # codex r10: unresolvable demand input -> EXCLUDED from rankings entirely (no ED row), named\n    # in a typed run-level exclusion record; `unresolved_input` is a CLOSED enum (no free text).\n    from demoflow.output.artifacts import rankings_document\n    doc = rankings_document([], {\"source_hashes\": {}}, \"h\", frozenset(),\n                            exclusions=[{\"geography\": \"HORS_RMR\", \"unresolved_input\": \"immigrant_component_flows\"}])\n    assert doc[\"rankings\"] == [] and doc[\"exclusions\"][0][\"geography\"] == \"HORS_RMR\"\n    with pytest.raises(ValueError, match=\"closed schema\"):\n        rankings_document([], {\"source_hashes\": {}}, \"h\", frozenset(),\n                          exclusions=[{\"geography\": \"HORS_RMR\", \"unresolved_input\": \"crash_probability=0.35\"}])\n```\n\n\n---\n\n## SEAT CARRIES — the plan body is defective here; these OVERRIDE it\n\n**A1 (the big one). `assert_no_open_strings` IS NOT A TREE-WALK, despite its name, its docstring and\nthe spec.** Spec §7 requires that **EVERY string-typed position in every emitted artifact — field\nvalues, enum members, and map KEYS — is either registry/enum-bound or format-validated.** The plan's\nfunction inspects exactly one path: `doc[\"data_vintage\"][\"source_hashes\"]`. Every other string in the\ndocument is unchecked. Its docstring nevertheless claims it implements the general rule. **A gate\ncarrying a false derivation is part of the defect** — run 28 corrected two of those. Either implement\nSpec §7's quantifier ranges over the WHOLE emitted artifact, so an uncovered string position\nviolates it however honestly the function is named — \"narrow the name and list what is uncovered\" is\nNOT an acceptable end state. Exactly two are: **(i)** implement the real recursive walk over the\nemitted document, or **(ii)** demonstrate full coverage BY COMPOSITION — the row validators plus the\nenvelope validator provably summing to EVERY string position in the document — bound by a test that\nfails when a new unvalidated string position is added. Do not ship the current mismatch.\n\n**A2. The exclusion record's `geography` value is UNBOUND — a live string side-channel.** The check is\n`set(exc) != {\"geography\", \"unresolved_input\"} or exc[\"unresolved_input\"] not in _UNRESOLVED_INPUTS`.\nThe KEY SET is bound and `unresolved_input` is enum-bound, but the `geography` VALUE is free text, so\n`{\"geography\": \"crash_probability=0.35\", \"unresolved_input\": \"immigrant_component_flows\"}` passes.\nThis is the same class as run 28's finding F3 (four string positions, one bound) and it must be\nclosed the same way: bind it to the `Geography` enum. RED-test the smuggle.\n\n**A3. `_ISO_DATE` validates SHAPE, not date.** `^\\d{4}-\\d{2}-\\d{2}([T ].*)?$` accepts\n`9999-99-99`. Parse it; a regex is not a calendar.\n\n**A4. `open(path, \"w\")` has no `encoding=`.** The artifacts carry accented French geography tokens\n(`Montréal`, `Québec`). On any non-UTF-8 locale this mojibakes or raises, and the failure is\nenvironmental so the suite will not show it. Pin `encoding=\"utf-8\"`.\n\n**A5. Keep what is already right.** `sort_keys=True` + `allow_nan=False` + the pre-write finiteness\nwalk are correct and load-bearing — do not \"simplify\" them away. `_assert_finite` recurses values but\nnever inspects dict KEYS; note it if you leave it.\n\n**A6. Task-30 constraint, recorded because it shapes what you emit here.** IRCC restates history and\nthe ISQ workbooks are re-published, so a golden pinning computed values reds on upstream refresh with\nNO code change. Whatever the envelope carries must make a red **attributable to data-vs-code**. This\nis exactly what task 2's `assumptions_hash` defect breaks — see that mandate — so build the envelope\nso a correct hash can be dropped in without reshaping the document.\n\nSEAT-RUN ADDENDUM (binding): commit NOTHING — the seat commits at run close after its own diff review. RED-first binds. Run-unique scratchpad subdir; PYTHONDONTWRITEBYTECODE=1 for mutation batteries; probe on COPIES and never disable a guard in the live tree. Finish with `./scripts/test-all.sh` from the worktree root; real tail, figures derived from that run and never transcribed.",
   "money_path": false,
   "load_bearing": true
  },
  {
   "title": "Task 29b — pipeline.py orchestrator (kills the re-introduced false-green; consumes the aligned rho join)",
   "instructions": "### Task 29b: `pipeline.py` — the orchestrator (SECOND of three; `artifacts.py` from task 1 exists)\n\n**Plan bytes for this file, extracted mechanically:**\n\n`demoflow/src/demoflow/pipeline.py`:\n```python\n\"\"\"Tranche-1 pipeline (spec §5/§6/§7, rounds 7–9). OwnerStock via the §7 defining equation;\nnative formation on P_resident (I2 operand binding + P_resident≥0); dimensional immigrant chain;\nRUN-CONTRACT central values + rank_stable sweep; projected-years-only ranking domain; identity\nenvelope + source_hashes. COARSE by design (T1). YSL / beta mapping are Tranche 2.\"\"\"\nimport hashlib\nfrom pathlib import Path\n\nimport pandas as pd\n\nfrom demoflow.balance.excess_demand import excess_demand\nfrom demoflow.balance.owner_stock import owner_stock\nfrom demoflow.cohort.basis import q_at\nfrom demoflow.cohort.init import initialize_households\nfrom demoflow.cohort.listings import ESTATE_LAG_YEARS, market_listings\nfrom demoflow.cohort.rollforward import Stock, roll_one_year\nfrom demoflow.demand.formation import immigrant_formation, native_formation, total_owner_demand\nfrom demoflow.demand.i2 import assert_i2_identity, assert_p_resident_nonneg, p_resident\nfrom demoflow.demand.immigrant_inputs import resolve_immigrant_inputs\nfrom demoflow.geography import Geography, RA_PROXY_MEMBERS, Scenario\nfrom demoflow.loaders.census import (\n    headship_rate, load_headship_rates, load_ownership_rates, ownership_rate,\n)\nfrom demoflow.loaders.compo import load_immigrant_flows\nfrom demoflow.loaders.constants import CENTRAL_ASSUMPTIONS, CONSTANTS, SWEEP_GRID, assumptions_hash\nfrom demoflow.loaders.ircc import load_pr_landings\nfrom demoflow.loaders.isq import load_population\nfrom demoflow.loaders.living_arrangement import couple_share, living_alone_rate, load_living_arrangement\nfrom demoflow.loaders.pins import DATA_DIR\nfrom demoflow.output.artifacts import rankings_document, tripwire_document, write_json_strict\nfrom demoflow.output.rankings import rank_geographies, refuse_cross_vintage\nfrom demoflow.output.tripwires import (\n    SourceKind, TripwireSpec, check_registry, evaluate_indicator, exit_code,\n)\n\nHORIZON_YEARS = [2030, 2035, 2040, 2045, 2050]   # ScenarioPrior horizons (T2 reference), NOT the ranking domain\n_POP_WORKBOOKS = [\"pop-as-rmr-base.xlsx\", \"pop-as-ra-base.xlsx\"]\n_COMPO_WORKBOOKS = [\"compo-rmr-base.xlsx\", \"compo-ra-base.xlsx\"]\nALLOWED_SOURCE_KEYS = frozenset([\"ownership_by_geo_age.json\", \"headship_by_age.json\", \"living_arrangement.json\"])\n# codex r10: geographies whose demand-side inputs are unresolvable (HORS_RMR fallback iii) are\n# EXCLUDED FROM RANKINGS ENTIRELY (no ED). Populated at probe P5/P6; EMPTY in the committed run\n# (HORS_RMR's immigrant flows resolve from the compo hors-RMR row / province residual).\nEXCLUDED_FROM_RANKINGS: frozenset = frozenset()\n\n\ndef _load_all(data_dir: Path | None):\n    pop = pd.concat([load_population(w, data_dir=data_dir) for w in _POP_WORKBOOKS], ignore_index=True)\n    compo = pd.concat([load_immigrant_flows(w, data_dir=data_dir) for w in _COMPO_WORKBOOKS], ignore_index=True)\n    return pop, compo, load_ownership_rates(data_dir=data_dir), \\\n        load_headship_rates(data_dir=data_dir), load_living_arrangement(data_dir=data_dir)\n\n\ndef _projected_years(pop_g_s: pd.DataFrame) -> list[int]:\n    \"\"\"Ranking temporal domain (codex r8-F3): projected years only (Statut=proj), the full\n    contiguous annual lattice through the last projected year (2051), both endpoints included.\"\"\"\n    proj = pop_g_s[pop_g_s[\"status\"].astype(str).str.lower().str.startswith(\"proj\")]\n    return sorted(int(y) for y in proj[\"year\"].unique())\n\n\ndef _pop_by_age(pop_g_s, year): return {int(a): float(v) for a, v in\n                                        pop_g_s[pop_g_s[\"year\"] == year].groupby(\"age\")[\"population\"].sum().items()}\ndef _ownership_by_age(ownership, geo): return {a: ownership_rate(ownership, geo, a) for a in range(25, 101)}\ndef _headship_by_age(headship): return {a: headship_rate(headship, a) for a in range(0, 101)}\n\n\ndef _init_stock(pop_g_s, year, ownership, la, geo) -> Stock:\n    r = pop_g_s[(pop_g_s[\"year\"] == year) & (pop_g_s[\"age\"] >= 75)]\n    pop_by_sex = {s: float(r[r[\"sex\"] == s][\"population\"].sum()) for s in (\"M\", \"F\")}\n    h = initialize_households(\n        pop_by_sex,\n        living_alone_rate_by_sex={s: living_alone_rate(la, geo, 80, s) for s in (\"M\", \"F\")},\n        couple_share_by_sex={s: couple_share(la, geo, 80, s) for s in (\"M\", \"F\")},\n        collective_share=CONSTANTS[\"collective_share_75plus\"].value,\n        ownership_rate=ownership_rate(ownership, geo, 80),\n    )\n    return Stock(couple=h.owner_couple, solo_m=h.owner_solo_m, solo_f=h.owner_solo_f)\n\n\ndef _ed_series(geo, scen, pop, compo, ownership, headship, la, q_live) -> list[float]:\n    \"\"\"ED at every PROJECTED year (the ranking domain). q_live is passed so the rank_stable\n    sweep can re-run at the band endpoints; the headline run uses the central value.\"\"\"\n    inp = resolve_immigrant_inputs(geo)\n    p_nonimm = ownership_rate(ownership, geo, 40)\n    hs, own = _headship_by_age(headship), _ownership_by_age(ownership, geo)\n    pop_g_s = pop[(pop[\"geography\"] == geo) & (pop[\"scenario\"] == scen)]\n    compo_g_s = compo[(compo[\"geography\"] == geo) & (compo[\"scenario\"] == scen)]\n    years = _projected_years(pop_g_s)\n    base_year = int(pop_g_s[\"year\"].min())\n\n    stock = _init_stock(pop_g_s, base_year, ownership, la, geo)\n    estate, voluntary = {}, {}\n    for year in range(base_year, years[-1] + 1):\n        nxt, exits = roll_one_year(stock, age=80, year=year, q_live=q_live, qx=q_at)\n        entrants = _init_stock(pop_g_s, min(year + 1, int(pop_g_s[\"year\"].max())), ownership, la, geo)\n        inflow = max(entrants.owner_units - stock.owner_units, 0.0) * 0.1\n        stock = Stock(nxt.couple + inflow, nxt.solo_m, nxt.solo_f)\n        estate[year], voluntary[year] = exits[\"estate\"], exits[\"living\"]\n    listings = market_listings(voluntary, estate, lag=ESTATE_LAG_YEARS)\n\n    def _scale(year):\n        p_isq = float(pop_g_s[pop_g_s[\"year\"] == year][\"population\"].sum())\n        surviving = [float(compo_g_s[compo_g_s[\"year\"] == y][\"immigrants_permanents\"].sum())\n                     for y in range(base_year, year + 1)]\n        p_res = assert_p_resident_nonneg(p_resident(p_isq, surviving), ctx=f\"{geo.value}/{scen.value}/{year}\")\n        return (p_res / p_isq if p_isq > 0 else 1.0), p_isq, surviving\n\n    series = []\n    for t in years:\n        scale_t, p_isq_t, surviving_t = _scale(t)\n        scale_tm1, _, _ = _scale(t - 1)\n        resident_t = {a: p * scale_t for a, p in _pop_by_age(pop_g_s, t).items()}\n        resident_tm1 = {a: p * scale_tm1 for a, p in _pop_by_age(pop_g_s, t - 1).items()}\n        assert_i2_identity(sum(resident_t.values()), p_isq_t, surviving_t)   # operand binding\n        D = total_owner_demand(\n            native_formation(resident_t, resident_tm1, hs, own),\n            immigrant_formation(float(compo_g_s[compo_g_s[\"year\"] == t][\"immigrants_permanents\"].sum()),\n                                inp.immigrant_headship, p_nonimm, inp.ownership_ratio))\n        os = owner_stock(_pop_by_age(pop_g_s, t), hs, own)   # §7 defining equation\n        series.append(excess_demand(D, listings.get(t, 0.0), os))\n    return series\n\n\ndef _ed_dict(geos, pop, compo, ownership, headship, la, q_live):\n    return {g: {sc: _ed_series(g, sc, pop, compo, ownership, headship, la, q_live)\n                for sc in (Scenario.REFERENCE, Scenario.LOW, Scenario.HIGH)} for g in geos}\n\n\ndef _rank_stability(geos, pop, compo, ownership, headship, la) -> dict:\n    \"\"\"RUN-CONTRACT robustness sweep (codex r8-F1): a geography's rank is STABLE iff it is\n    unchanged at both q_live band endpoints vs the central value.\"\"\"\n    lo, hi = SWEEP_GRID[\"q_live_per_year\"]\n    orders = [ {r.geography: r.rank for r in rank_geographies(_ed_dict(geos, pop, compo, ownership, headship, la, q))}\n               for q in (CENTRAL_ASSUMPTIONS[\"q_live_per_year\"], lo, hi) ]\n    return {g: all(o[g] == orders[0][g] for o in orders) for g in geos}\n\n\ndef _source_hashes(data_dir: Path | None) -> dict:\n    dd = data_dir or DATA_DIR\n    return {name: {\"sha256\": hashlib.sha256((dd / name).read_bytes()).hexdigest(),\n                   \"extracted_at\": \"2026-07-21\"}\n            for name in ALLOWED_SOURCE_KEYS if (dd / name).exists()}\n\n\ndef _vintage(data_dir: Path | None) -> dict:\n    return {\"isq_edition\": \"mise a jour 2026\", \"census_year\": 2021, \"constants_as_of\": \"2026\",\n            \"source_hashes\": _source_hashes(data_dir)}\n\n\ndef _tripwire_results(now_year: int) -> list:\n    pr = load_pr_landings()\n    specs = [\n        (TripwireSpec(\"pr_landings_annual\", 40000.0, 50000.0, 2026, 1, SourceKind.WIRED),\n         (45000.0 if pr.available else None), pr.available),\n        (TripwireSpec(\"temp_resident_stock\", 0.0, 1e9, 2026, 1, SourceKind.WIRED), None, False),\n        (TripwireSpec(\"isq_edition_watch\", 2025.5, 2027.5, 2026, 1, SourceKind.WIRED), 2026.0, True),\n        (TripwireSpec(\"registre_foncier_volume\", 0.0, 1e9, 2026, 1, SourceKind.OPERATOR_SUPPLIED), None, True),\n        (TripwireSpec(\"cmhc_senior_sale_5yr\", 0.30, 0.42, 2021, 5, SourceKind.OPERATOR_SUPPLIED),\n         CONSTANTS[\"cmhc_senior_sale_5yr\"].value, True),\n        (TripwireSpec(\"natural_increase_sign\", -1e9, 1e9, 2026, 1, SourceKind.OPERATOR_SUPPLIED), -1200.0, True),\n    ]\n    results = [evaluate_indicator(s, v, available=a, now=now_year) for s, v, a in specs]\n    results += check_registry([s.indicator for s, _, _ in specs])   # completeness (empty here)\n    return results\n\n\ndef run_pipeline(data_dir: Path | None = None, out_dir: Path | None = None, now_year: int = 2026):\n    pop, compo, ownership, headship, la = _load_all(data_dir)\n    ah = assumptions_hash()\n    refuse_cross_vintage({ah})   # identity envelope drives the same-vintage refusal\n\n    present = [g for g in Geography if g in set(pop[\"geography\"].unique())]\n    geos = [g for g in present if g not in EXCLUDED_FROM_RANKINGS]   # excluded geos carry NO ED\n    exclusions = [{\"geography\": g.value, \"unresolved_input\": \"immigrant_component_flows\"}\n                  for g in present if g in EXCLUDED_FROM_RANKINGS]\n    ed = _ed_dict(geos, pop, compo, ownership, headship, la, CENTRAL_ASSUMPTIONS[\"q_live_per_year\"])\n    borrowed = {g for g in geos if g in RA_PROXY_MEMBERS or g is Geography.HORS_RMR}\n    stable = _rank_stability(geos, pop, compo, ownership, headship, la)\n    rankings = rank_geographies(ed, borrowed=borrowed, rank_stable=stable)\n\n    trips = _tripwire_results(now_year)\n    vintage = _vintage(data_dir)\n    out_dir = Path(out_dir) if out_dir else Path.cwd() / \"artifacts\"\n    out_dir.mkdir(parents=True, exist_ok=True)\n    write_json_strict(out_dir / \"rankings.json\",\n                      rankings_document(rankings, vintage, ah, ALLOWED_SOURCE_KEYS, exclusions=exclusions))\n    write_json_strict(out_dir / \"tripwire_baseline.json\", tripwire_document(trips, vintage, ah, ALLOWED_SOURCE_KEYS))\n    return {\"rankings\": rankings, \"tripwires\": trips, \"exit_code\": exit_code(trips), \"out_dir\": out_dir}\n```\n\n\nIts plan test cases are `test_run_pipeline_emits_two_json_artifacts_with_identity_envelope`,\n`test_horizons_are_the_declared_set` and `test_two_vintage_mixing_refused` from task 1's test block.\n\n---\n\n## SEAT CARRIES — TEN defects the seat measured in this body before dispatch. They OVERRIDE it.\n\n**B1. THE HEADLINE: this body RE-INTRODUCES the exact false-green that Task 28 and run 29 exist to\nkill, and it must not ship.** `_tripwire_results` builds\n`TripwireSpec(\"pr_landings_annual\", 40000.0, 50000.0, 2026, 1, SourceKind.WIRED)` and feeds it\n**`45000.0 if pr.available else None`** — a hardcoded literal sitting inside its own band, compared\nagainst that band, green forever, on any data, in any era. It never calls `evaluate_pr_landings`, so\nthe measured-realized path, the plan-era gate, the degenerate-feed floor, the freshness gate and\nruling U's member-set completeness contract are ALL bypassed. **Delete the literal and wire\n`evaluate_pr_landings`.** Its `PRLandingsEvaluation` carries `result` (the only thing that may reach\nthe artifact), plus `realized`, `vintage_sha256` and `log` for the run. Live state today is UNKNOWN /\n`source_unavailable` / exit 1 because no plan-governed year has closed — **that is the CORRECT\nanswer and the pipeline must be able to emit it**, not route around it.\n\n**B2. Use `run_exit_code`, NOT `exit_code`.** Both `run_pipeline` and the plan's CLI call `exit_code`,\nwhich ranges only over the results it is HANDED — one OK result exits 0, however short of the\nrequired set. `run_exit_code` is the gate that requires the evaluated indicator multiset to EQUAL the\ncode-owned required set. It has NO production consumer today; this is where it gets one, or run 28's\nfinding F4 stays half-closed and the coverage question goes unasked.\n\n**B3. CONSUME THE ALIGNED ρ JOIN — or four earlier runs were decoration.** Verified by the seat:\nnothing under `src/` imports `hors_aligned` except comments. `_ed_series` reads\n`_ownership_by_age(ownership, geo)`, the CONTAMINATED curve, so excess demand is still computed from\nthe operand-misaligned rates that ruling #12(B) was reversed to fix. `loaders/hors_aligned.py`\n(`load_aligned_ownership_rates`, `load_aligned_ownership_join`, `aligned_ownership_rate`) exists,\nis tested, and is consumed by NOTHING. Wire it. If you find a reason it cannot be wired, STOP and\nreport — that is a seat decision, not an implementer's.\n\n**PRE-RULED so you neither halt nor quietly skip the wiring: a RED on a committed ED oracle after you\nwire the join is EXPECTED, not a regression.** The aligned curve changes ED's inputs by construction.\nRe-derive the affected oracle BY HAND against the aligned inputs and show the derivation — **never\ntranscribe the new code's output back into the oracle**, which turns a test into a mirror. Report the\ndivergence as plan-superseded, with the before/after numbers.\n\n**B4. The identity envelope cannot do the one thing it claims.** `refuse_cross_vintage({ah})` is\ncalled with a ONE-element set built from the hash just computed, so it can never fire. Worse,\n`assumptions_hash()` takes no arguments and hashes only `CENTRAL_ASSUMPTIONS` + `SWEEP_GRID` — **the\nDATA vintage is not in it at all**, so two runs over different source bytes produce an identical\nhash. The envelope's whole purpose is making a red attributable to data-vs-code. Fix the hash to\ncover the data vintage, and either give `refuse_cross_vintage` a real multi-vintage input or delete\nthe vacuous call — **a gate that cannot fail is not a gate.**\n\n**B5. `_source_hashes` SILENTLY DROPS a missing source.** `for name in ALLOWED_SOURCE_KEYS if\n(dd / name).exists()` — an absent input vanishes from the vintage instead of refusing. Absence\ndiscipline: an empty listing is data, never proof. Refuse, naming the missing file.\n\n**B5b. THE IDENTITY ENVELOPE COVERS 3 OF 13 COMMITTED INPUTS — seat-measured.** `ALLOWED_SOURCE_KEYS`\ndeclares `ownership_by_geo_age.json`, `headship_by_age.json`, `living_arrangement.json`. All three\nexist, so the `.exists()` filter drops nothing TODAY — but `git ls-files demoflow/data/` returns\n**thirteen** inputs, and the ten uncovered ones include **every population and immigrant-flow\nworkbook** (`pop-as-rmr-base.xlsx`, `pop-as-ra-base.xlsx`, `pop-as-qc-base.xlsx`,\n`compo-rmr-base.xlsx`, `compo-ra-base.xlsx`) plus `census_tenure_age_98100231.csv`,\n`hors_aligned_csd_98100232.json` and `ownership_hors_aligned.json`. The envelope's claim is that a\nred is attributable to data-vs-code; for ten inputs — including the ones that drive the entire model\n— it cannot make that call. **And carry B3 makes this worse in the same commit:** consuming the\naligned join adds `ownership_hors_aligned.json` as a live model dependency that the envelope does not\ncover. Widen the coverage to every input the run actually reads, and make an input read-but-unhashed\na REFUSAL rather than a silent omission.\n\n**B6. `\"extracted_at\": \"2026-07-21\"` is a hardcoded date literal** stamped on every source hash\nregardless of when the file was extracted. Derive it or carry it as declared provenance — do not\nfabricate a per-file fact.\n\n**B7. THREE OF SIX TRIPWIRE BANDS ARE VACUOUS.** `temp_resident_stock` [0, 1e9],\n`registre_foncier_volume` [0, 1e9], and `natural_increase_sign` [−1e9, 1e9] — the last fed a live\n−1200.0, so it reports **OK forever by construction**. These are decorative greens in a fail-safe\ngate, which is worse than an absent indicator because they read as verified. **A BAND IS A RULED VALUE, NOT AN IMPLEMENTER'S JUDGMENT CALL** — this module's own header says\nbands arrive from the constants/spec surface and are never invented in the module. So: if a real band\nfollows from an EXISTING cited constant, use it and cite it; if it does not, the in-lane answer is\n**UNKNOWN with a closed reason**, and proposing a NEW band constant is a **SEAT_QUESTION**, not a\ncall you make.\n\n**And `natural_increase_sign`'s `-1200.0` is itself a fabricated operator input** — a hand-typed\nliteral standing in for a measurement nobody took, the identical depth-1 class as the `45000.0`. An\noperator-supplied indicator with no real input carries `None`, which the gate already turns into\n`OPERATOR_INPUT_MISSING`. **Expect this to change the artifact contents AND the run's exit code —\nthat is the fix working, not a regression.** Report it rather than preserving a green.\n\n**B8. `inflow = max(entrants.owner_units - stock.owner_units, 0.0) * 0.1` — an uncited magic 0.1 on\nthe model path**, with no derivation in the spec, the plan, or the constants surface. Every other\nmodel constant in this tree is cited or carries an anchor. Either source it into `constants.py` with\nprovenance or report it as an open input.\n\n**B9. `_scale(t - 1)` at the base year reaches a year the frame does not contain.** `surviving` ranges\nover an empty span and `p_isq` falls through to the `if p_isq > 0 else 1.0` branch — a SILENT scale\nof 1.0 rather than a refusal. Same class as the deflating door below.\n\n**B10. The `borrowed` set is derived from the wrong thing.** `borrowed = {g for g in\ngeos if g in RA_PROXY_MEMBERS or g is Geography.HORS_RMR}` marks RA proxies `borrowed_prior` when\n`rankings.py` ALREADY adds `ra_proxy` for exactly that set — one geography, two flags, for one fact.\n`borrowed_prior` answers a DIFFERENT question: were this geography's INPUTS borrowed from a coarser\nprior? That answer already exists PER FIELD on `ImmigrantInputs` (`headship_provenance` /\n`ratio_provenance`, closed vocabulary `{cited, borrowed_prior, computed_residual}`) — under ruling Q\na geography can carry a CITED ratio and a BORROWED headship, which a hardcoded geo set cannot\nexpress. **Derive the flag from that provenance surface.**\n\n**Do NOT merge the vocabularies.** `INPUT_PROVENANCE` and the rankings flags enum deliberately share\nthe token `borrowed_prior` while remaining separate sets — `immigrant_inputs.py` and `rankings.py`\nboth say so in their own comments, and `constants.ANCHOR_FLAGS` is a third. Four closed vocabularies,\nnone of them each other.\n\n**RULING K IS ALREADY WIRED — do not re-implement it, and do not break it.** Seat-verified before\ndispatch: `rankings.py` carries `CLOSED_COHORT_EXCEEDANCE_MEMBERS = frozenset({Geography.LAVAL_RA13})`\nand emits `closed_cohort_exceedance` on every row of that geography. A pipeline change that stops\nthat flag riding a LAVAL_RA13 row is a defect.\n\n**B11. The pipeline test binds to REAL data.** `run_pipeline(out_dir=tmp_path, now_year=2026)` passes\nno `data_dir`, so it silently uses the committed `DATA_DIR` and the suite's result depends on those\nbytes. Make the data source explicit in the test.\n\n## Standing carries that land HERE (routed from earlier runs)\n\n- **The deflating door:** an empty or holed `resident_pop_t` yields `D_native = 0.0` SILENTLY. A\n  missing population is not zero demand — refuse.\n- **The exits→cause mapping:** `rollforward` emits `living`, `phi_market` accepts `voluntary`. Today\n  that is fail-loud; wiring them is this task's job, and the mapping must be explicit, not positional.\n- **Ruling O is an ACCEPTANCE CONDITION, not a nicety:** the reconciliation gate binds the\n  CENTRAL-ASSUMPTION run ONLY — sweep legs never re-run `check_reconciliation`. Binding every leg\n  makes the spec self-contradictory (measured: at q_live 0.06 the spec-pinned cohort RAISES while a\n  doubled decrement PASSES, inverted at 21/21 start years). **But state it as an OBLIGATION first:**\n  the plan's pipeline body never imports or calls `check_reconciliation` AT ALL (seat-verified —\n  absent from plan:4612-4801; it lives at `cohort/gates.py:53`), so a carry phrased only as \"don't\n  let a sweep leg trip it\" is satisfied by calling it NOWHERE — a gate that cannot fail, the very\n  class this task is full of. **The CENTRAL-ASSUMPTION run MUST invoke `check_reconciliation`; the\n  sweep legs MUST NOT.** RED case required: a central run with a reconciliation-violating input\n  REFUSES.\n- **HORS_RMR IS RANKED.** Spec §7's exclusion branch (unresolvable component flows → excluded\n  entirely, no partial ED) did NOT fire: `compo-rmr-base.xlsx` carries its own hors-RMR row. So\n  `EXCLUDED_FROM_RANKINGS` is legitimately EMPTY — but the exclusion PATH must exist and be tested,\n  because a future vintage without that row must exclude rather than emit a partial ED.\n- **The enum serialization trap, measured twice in this arc.** `str(Geography.MTL_RMR)` yields\n  `'Geography.MTL_RMR'`, not `'MTL_RMR'` — emitters use `.value`. `Series.astype(str)` on an enum\n  column yields the REPR, so a string filter matches NOTHING and returns an empty frame rather than\n  raising. **Verified non-issue for `status`** (it holds plain strings `r`/`p`/`proj`, so\n  `_projected_years` is safe) — the trap is live for `geography` and `scenario` columns.\n\nSEAT-RUN ADDENDUM (binding): commit NOTHING — the seat commits at run close after its own diff review. RED-first binds. Run-unique scratchpad subdir; PYTHONDONTWRITEBYTECODE=1 for mutation batteries; probe on COPIES and never disable a guard in the live tree. Finish with `./scripts/test-all.sh` from the worktree root; real tail, figures derived from that run and never transcribed.",
   "money_path": false,
   "load_bearing": true
  },
  {
   "title": "Task 29c — cli.py + the console script that has never run",
   "instructions": "### Task 29c: `cli.py` — and the console script that has NEVER run (THIRD of three)\n\n**Plan bytes for this file, extracted mechanically:**\n\n`demoflow/src/demoflow/cli.py`:\n```python\n\"\"\"demoflow CLI (spec §3): `demoflow run` emits rankings + tripwire artifacts;\n`demoflow tripwires` re-evaluates tripwires and sets the exit code (0 iff all OK).\"\"\"\nimport argparse\nimport sys\nfrom pathlib import Path\n\nfrom demoflow.pipeline import run_pipeline\nfrom demoflow.output.tripwires import exit_code\n\n\ndef main(argv: list[str] | None = None) -> int:\n    parser = argparse.ArgumentParser(prog=\"demoflow\")\n    sub = parser.add_subparsers(dest=\"cmd\", required=True)\n    for name, help_ in [(\"run\", \"emit rankings + tripwire artifacts\"), (\"tripwires\", \"evaluate tripwires\")]:\n        p = sub.add_parser(name, help=help_)\n        p.add_argument(\"--out\", type=Path, default=Path(\"artifacts\"))\n    args = parser.parse_args(argv)\n\n    result = run_pipeline(out_dir=args.out)\n    if args.cmd == \"run\":\n        print(f\"wrote {args.out}/rankings.json and {args.out}/tripwire_baseline.json\")\n        return 0\n    for t in result[\"tripwires\"]:\n        reason = f\" — {t.reason.value}\" if t.reason else \"\"\n        print(f\"{t.status.value:8} {t.indicator} ({t.source}){reason}\")   # t.source is the declared string\n    return exit_code(result[\"tripwires\"])\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())\n```\n\n\n- [ ] **Step 4: Run to verify it passes**\n\nRun: `cd demoflow && uv run pytest tests/test_pipeline.py -v`\nExpected: 6 PASS. (Coarse-model cautions: the rank_stable sweep runs the ED grid three times — if an\nED denominator drops below the 1,000-household guard, or the couple balance gate raises on the\ncommitted per-sex rates, calibrate `living_arrangement.json` per-geo from P3 so `coupled_m ≈\ncoupled_f`; the golden pins whatever the calibrated vintage produces — no exact ED numbers asserted.)\n\n- [ ] **Step 5: Commit**\n\n```bash\ngit add demoflow/src/demoflow/output/artifacts.py demoflow/src/demoflow/pipeline.py demoflow/src/demoflow/cli.py demoflow/tests/test_pipeline.py\ngit commit -m \"feat(demoflow): pipeline identity envelope + projected-year domain + rank_stable sweep + P_resident>=0 + no-open-string (§7/§9 r7/r8/r9-F)\"\n```\n\n---\n\n\n---\n\n## SEAT CARRIES\n\n**C1. THE CONSOLE SCRIPT IS BROKEN RIGHT NOW — the seat verified it live, not by reading.**\n`pyproject.toml:16` declares `demoflow = \"demoflow.cli:main\"` and `src/demoflow/cli.py` does not\nexist:\n\n```\n$ uv run demoflow --help\nModuleNotFoundError: No module named 'demoflow.cli'\n```\n\n**ACCEPTANCE IS A LIVE RUN, NOT A TEST.** Before you report done, actually execute\n`uv run demoflow --help`, `uv run demoflow run --out <a tmp dir>` and `uv run demoflow tripwires`\nfrom `demoflow/`, and paste the real output with the real exit codes. A unit test that imports `main`\nand passes `argv` does NOT discharge this — the entry point, the package metadata and the installed\nconsole script are the boundary, and a test mocks exactly the leg that is broken. Every boundary, hit\nlive.\n\n**C2. `demoflow tripwires` recomputes the ENTIRE model to print six statuses.** `main` calls\n`run_pipeline(...)` for BOTH subcommands, and `run_pipeline` runs the full ED grid plus a\n`_rank_stability` sweep that runs it three more times. The tripwire subcommand needs the tripwire\nevaluation, not the demographic model. Give it a path that does not build rankings.\n\n**C3. `main` writes artifacts for BOTH subcommands** — `demoflow tripwires` emits `rankings.json`\nas a side effect of being asked for a status listing. Separate the emission from the evaluation.\n\n**C4. `run` returns 0 unconditionally**, discarding the tripwire verdict entirely, and `tripwires`\nreturns `exit_code(...)` rather than `run_exit_code(...)` (see task 2, carry B2). **The seat RULES this\ncontract rather than leaving it to you, because it is an interface promise:** `demoflow run` exits on\n**EMISSION success** — artifacts written and every contract validated — and `demoflow tripwires`\nexits **`run_exit_code(...)`**, matching spec §7c's \"exit 0 only when every code-required indicator\nis present exactly once, finite, fresh, well-banded, and OK\", which is a property of the tripwire\nevaluation, not of the emission. **Both meanings go in the `--help` text** — an exit code no operator\ncan look up is a gate nobody can rely on.\n\n**C5. `t.source` is the DECLARED registry string** and is printed per line; keep it bound. Reason\ntokens print from the closed enum (`t.reason.value`), never free text.\n\nSEAT-RUN ADDENDUM (binding): commit NOTHING — the seat commits at run close after its own diff review. RED-first binds. Run-unique scratchpad subdir; PYTHONDONTWRITEBYTECODE=1 for mutation batteries; probe on COPIES and never disable a guard in the live tree. Finish with `./scripts/test-all.sh` from the worktree root; real tail, figures derived from that run and never transcribed.",
   "money_path": false,
   "load_bearing": true
  }
 ]
}