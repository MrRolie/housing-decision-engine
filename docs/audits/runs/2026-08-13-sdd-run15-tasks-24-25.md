# Seat-run dispatch record — run 15 (Tasks 24/25 — transfer split + demand side)

Successor to run 14 (`wf_feeda0d3-3c7`), which DIED in the 2026-08-08 power outage before any
stage landed. Workflow resume is same-session-only, so this is a FRESH dispatch derived
programmatically from run 14's committed args — never re-typed.

- date: 2026-08-13
- script: `mm-spine/.claude/workflows/mm-sdd-pipeline.js` sha256
  `d7743e42b0ebe6499f8f5af986b471ea3417aff067710048dc39aa8756e315ea` @ mm-spine `f4d7cca`
  (`node --check` clean at wrapper build). NOTE: this SUPERSEDES the handoff's pin
  `184f78b4…` — `f4d7cca` (the 2026-08-13 seat-tier ruling) rewrote two prefab strings
  (`whenToUse` tier clause + the load-bearing review-tier label). Dispatching the old pin
  would resurrect superseded tier prose; the seat dispatches current canonical HEAD bytes.
- args: `2026-08-13-sdd-run15-tasks-24-25.args.json` (sha256
  `4135b7b6b1c6f6fb1ed04d445125f36a5565f12781a3eebce3265061a7da7173`), derived from run 14's
  `a79e855794a8f81c4cbff9913b70c6122182526d7f946ddab998f1437c720cf3` (source sha re-verified
  byte-exact against run 14's record before editing)
- models: opus/opus; load_bearing ×2; money_path: false ×2; WAVE-0 vacuous (no board in HDE)
- preconditions (GROUND FIRST, seat's own run): tree clean, HEAD `a3c5da5` = run-14 record
  commit, `./scripts/test-all.sh` green at hde 191 + demoflow 348; both worktrees unoccupied
  (session-telemetry, scope=all — no session on either checkout)

## Pre-dispatch amendments (three defects found in run 14's committed args)

The handoff mandated verifying the interim session's carries against the charter before
dispatch. Three defects, all fixed programmatically with the diff verified:

1. **STALE TASK MAPPING (blocking).** The context block still described run 13's tasks —
   "task 1 = the seat-authored Task-22 completion carrying STEERING RULING N; task 2 = plan
   Task 23 verbatim" — while the tasks shipped were 24/25/25b. `context` interpolates into
   EVERY stage, so all implementers and reviewers would have read a false task mapping.
   Rewritten to the actual mapping.
2. **STALE BASELINE.** "green at dispatch: hde 191 + demoflow 336 (… committed: 348)" —
   replaced with the seat-measured `hde 191 + demoflow 348` at HEAD `a3c5da5`.
3. **MISPLACED CARRY (blocking).** The constants-redeclaration carry ("IMPORT from constants,
   never redeclare") was attached to **Task 25**, but the defect lives in `listings.py`, which
   **Task 24** creates — plan:3362-3364 sits inside Task 24's body (Task 24 opens at plan:3307,
   verified). The Task-24 implementer would never have seen it. Moved to Task 24 and
   strengthened: BIND read-through from `CENTRAL_ASSUMPTIONS`, plus a discriminating test
   (mutate the `CENTRAL_ASSUMPTIONS` entry, assert listings' value MOVES — a redeclared literal
   survives that mutation, which is exactly the defect). Mechanism cited from
   `constants.py`'s own docstring: `assumptions_hash` covers only `CENTRAL_ASSUMPTIONS`, so a
   second declaration site moves the run's numbers while the hash stays byte-identical,
   breaking spec:532 artifact identity. The misfile ORIGINATED in the charter/anchor carry
   list (filed under T25 there too) — corrected at both sources at this run's fold.

## Task 25b HELD (not dispatched)

Task 25b was cut from the batch. Its `immigrant_inputs.py` join table hardcodes five uncited
literals — immigrant headship 0.42/0.45/0.43 and ownership ratios 0.62/0.70/0.66 — which exist
NOWHERE but the plan's own code block (grepped: absent from the spec, absent from the tree).
That collides head-on with the charter carry enforced in code by `Anchor.__post_init__`: "every
constant carries its documented anchor; a constant without an anchor is a defect." Two value
decisions block it, neither inventable by an implementer:

- **Ownership ratio** — `constants.py`'s own `CENTRAL_PROVENANCE` records the selection as
  UNRULED and explicitly "a seat/operator call": P4 measured three defensible anchors (year-1
  0.210, year-3 0.614, year-5/settled 0.911, P4's non-binding recommendation) and "rules between
  NONE". The plan's 0.62 is not among them and carries no citation. Resolution path: check
  whether spec §6's temporal semantics ENTAIL the flow (arrival-window) or stock (settled)
  reading — if so the seat rules with file:line; if not it is an operator call. Note the I2
  interaction: native formation reads P_resident with arrivals excluded, so under a
  fresh-arrival rate an immigrant buying in year 6 lands in NEITHER channel.
- **Immigrant headship** — no source anywhere in the arc. Needs an isolated probe (P-series
  precedent); folds into the same operator ask if the probe comes back empty.

Queue: 25b (post-ruling) → 26 (+ the standing QFE debt).

## Advisor discipline (SESSION-TOPOLOGY §Seat-tier topology, ruled 2026-08-13)

- `ADVISOR RULING FIRED @pre-dispatch` — one call covering three dispositions: the args
  amendments above, the prefab-sha supersession, and the 25b hold. Cost (transcript-scale
  proxy, the stated currency): full-transcript forward at ~85k tokens of boot + verification
  context. Catch: confirmed all three defects as blocking/real and supplied the 25b
  disposition split (ratio = spec-or-operator, headship = probe) plus the successor-numbering
  and commit-before-dispatch ordering.

- run id: `wf_f75bae3a-8a2` (dispatched 2026-08-13; task id w2npgg6n5)
- outcome: **CLOSED CLEAN 2026-08-14 — 2/2 APPROVE** (T24 0 fix rounds, T25 2), whole-set verify
  PASS, 9 agents / 0 errors / 1.10M subagent tokens. Landed after the seat's own diff review as
  `309b86d` (Task 24) + `97359bb` (Task 25); suites **191 + 396** re-verified by the seat's own
  `./scripts/test-all.sh` run at the final tree (348 dispatch baseline + 9 test_listings + 36
  test_demand + 3 test_census_ownership = 396, reconciled to the unit by the verify stage).

## What the run caught

**The dispatch carry worked as designed.** T24's read-through binding landed exactly as the
amended args specified, and the review verified the discriminating property INDEPENDENTLY across
9 mutant cells rather than on the implementer's word — module attrs, def-time defaults, function
body and function return all die. Coverage exceeds the docstring's claim of two surfaces. Had the
carry stayed misfiled on Task 25, the plan's literal redeclaration would have shipped: it passes
every equality read, so only the reload-and-mutate test discriminates.

**T25's HIGH finding was real and material:** `native_formation` silently multiplied POSITIVE
formation gains by 0.0 at ages 18-24 — the same silent-zero class the task closes one age higher —
measured at 59.5% of computed D_native on the real artifacts. Resolved correctly rather than
patched: the convention is UPSTREAM (census.py's `zero_support_note`, landed T13b), so the fix is
full disclosure — all seven sub-floor ages named, both arms parametrized (absent rate contributes
nothing; a SUPPLIED sub-floor rate is still used, so an extended curve is never swallowed), floor
pinned equal to `min(census._AGE_BANDS)`. The measured block prices itself honestly: 97.6% of the
59.5% is ONE band-entry artifact of the pipeline sketch reusing a BANDED headship curve at single
years of age (h jumps 0.0061 → 0.3954 at the 0-19 → 20-34 boundary); net of band-entry the drop is
1.42%, LOW/HIGH fans move it to 0.91%/2.33%. The probe then found 100% of computed D_native is
itself band-entry mass in all three fans — so no number from that wiring can price the convention,
and the question is ROUTED to the seat rather than settled. **Seat disposition: the reopen trigger
stands as written in the code — it reopens when Task 29 lands an age-resolved headship curve.**

Two more doors closed in the same class: an absent prior-year COHORT zeroed the stock a gain is
measured against (30.0 present vs 300.0 absent, measured), and a NaN population would have
vanished rather than propagated. Both raise; a stated ZERO is still accepted.

## SEAT EDIT at commit (recorded, not silent)

`formation.py` stated as PRESENT FACT that `demand/i2.py` asserts P_resident ≥ 0 upstream. That
module lands at Task 25b, which this seat is HOLDING — so the sentence was a true-sounding claim
the tree does not entail, this arc's own depth-2 class. Both the T25 reviewer (asides 19/29) and
the boundary ledger flagged it as an observation; the seat converted it to a marked forward
reference with the live situation stated, verified before writing: `isq.py:222-226` refuses
non-finite AND negative populations at load. The caveat is self-deleting — it instructs 25b's
implementer to remove it when i2.py lands.

## Carries, routed to their tasks

- **Task 25b:** make the i2 forward reference true and DELETE the caveat in `formation.py`. Plus
  the immigrant leg asserts NOTHING (negative arrivals → negative owner households; NaN
  propagates) — the §7 emitter backstop it defers to is unbuilt.
- **Task 29 (pipeline):** (a) the DEFLATING door — an empty or holed `resident_pop_t` yields
  `D_native = 0.0` silently; the guard's scope is the `a−1` inflating leg only, and the pipeline
  is where cohort completeness is knowable. (b) `extracted_at` is validated as non-empty string
  only; plan:4568 wants ISO-8601. (c) the exits→cause mapping (`rollforward` emits
  `{'living','estate'}`, `phi_market` accepts `{'voluntary','estate'}` — fail-loud today,
  `phi_market('living')` raises, but the mapping that makes it CORRECT lives here). (d) the
  age-resolved headship curve that reopens the sub-floor question. (e) **`pyproject.toml:16`
  declares the console script `demoflow = "demoflow.cli:main"` and `cli.py` does not exist yet
  (Task 29 creates it, plan:4440) — hit that boundary as the real consumer does: run the INSTALLED
  console script, not only `python -m` (Law 1b).**
- **New, unpoliced boundary:** `import demoflow` does NOT pull pandas, but `cohort.listings` and
  `demand.formation` both do. `test_import_direction` binds demoflow↔hde only — the new
  cohort/demand → loaders direction has no gate. Route to 25b or 29 as a one-test addition.
- **Task 26/27/30 minor:** `RateVintage` is a public frozen dataclass with no `__post_init__`
  (direct construction bypasses the name/digest pairing refusal); the Anchor load leg on a
  BORROWED geography holds by iteration but no test pins it; dropping a BAND within a geography
  loads clean on both accessors (caught at read, not load); `living_arrangement` still spells its
  date `ref_date`; `SWEEP_GRID` bands `phi_voluntary` but `market_listings` exposes no phi
  override, so a future phi sweep leg needs one.
- **MERGE-CUTOVER PRECISION (actuarial coordination):** the ledger measured that `mcp_server`
  resolves inside demoflow's venv to a MATERIALIZED, non-editable copy
  (`demoflow/.venv/.../mcp_server/__init__.py`) — an edit in the actuarial source repo is not seen
  without a re-sync. The post-T1 cutover therefore has to re-sync the venv, not merely swap the
  import; the only out-of-worktree read in the whole strace was uv resolving the path dep.
- **Known-and-accepted:** the artifact disk-WRITE leg is exercised by no test; the verify closed it
  without touching the tree by re-deriving in memory with the generators' exact serialization —
  byte-identical to both committed files (5573=5573, 5472=5472), proving the committed bytes are
  generator output rather than hand edits. Same disclosed shape as run 6's offline-identity leg.

## Advisor discipline

- `ADVISOR RULING FIRED @pre-dispatch` — recorded above (one call, three dispositions).
- `ADVISOR RULING SKIPPED @run-15 fold — the seat's two dispositions here cite standing rules by
  file:line: the forward-reference correction applies the charter's depth-2 probe-discipline rule
  (charter §Probe discipline, "every gloss/token/inference must be a FUNCTION of what the code
  measured") and `constants.py`'s own "a second declaration site is a defect"; the sub-floor
  reopen trigger was left exactly as the run wrote it, which is not a new ruling.`
