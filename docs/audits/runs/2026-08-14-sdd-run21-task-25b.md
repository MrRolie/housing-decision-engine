# Seat-run dispatch record — run 21 (Task 25b: I2 + the immigrant-input join table)

The task the whole probe chain was built to make buildable. Both of its value questions are ruled
(S, T), its gate is corrected (#11), its numbers are measured and gated by a committed note (P8,
`44f3519`), and the catalogue search behind them is closed and claimable (P9, `bb89161`).

- date: 2026-08-14
- script: `mm-spine/.claude/workflows/mm-sdd-pipeline.js` sha256
  `d7743e42b0ebe6499f8f5af986b471ea3417aff067710048dc39aa8756e315ea` @ mm-spine `f4d7cca`
- args: `2026-08-14-sdd-run21-task-25b.args.json` (sha256
  `8bc6b0123f879435a1aec4a4696277e96b91b8343c2b2f1e29f42ea8dc917725`)
- models: opus/opus; load_bearing ×1; money_path: false; WAVE-0 vacuous
- preconditions (GROUND FIRST, seat's own run): tree CLEAN at HEAD `daef951`, `./scripts/test-all.sh`
  green at hde 191 + demoflow **494**, stable across repeat runs with zero skips; checkout free

## Mandate shape — a SPLIT mandate, deliberately

The plan's Task 25b predates five spec amendments. Its `i2.py` half is untouched by any of them and
is implemented plan-verbatim; its `immigrant_inputs.py` half is REWRITTEN under rulings S/T, with the
uncited literals voided and the plan's test bodies re-derived. The mandate says this explicitly
rather than leaving an implementer to reconcile plan bytes against five amendments alone — the
`MTL_RMR.flag is None` assertion alone changed semantics twice.

**Citation coupling is the point of the whole chain**, so it is mandated in terms: the join table's
values anchor against P8's `DECISION-HEADSHIP` / `DECISION-RATIO` tokens on the DIGITS, never a prose
prefix, proven three ways (fires on the defect, fires on defect+rewording, green on rewording alone).
A bare literal would make the entire measurement chain that produced it decorative.

## SEAT RULING — the join table's provenance vocabulary

`ImmigrantInputs` carries provenance **PER FIELD**: under ruling T a geography can hold a cited ratio
beside a borrowed headship, and one flag cannot say that honestly. The vocabulary is a closed
three-member set and is the JOIN TABLE'S OWN — neither `constants.ANCHOR_FLAGS` nor either of §7's
emitter enums:

- `cited` — measured directly for THIS geography (MTL_RMR, QC_RMR, RA06, RA13)
- `borrowed_prior` — the parent CMA's value standing in (RA14/15/16)
- `computed_residual` — arithmetic over cited counts at a CONSTRUCTED territory (HORS_RMR)

HORS_RMR is the reason the third member is minted: it must not be `cited`, because its territory is
constructed rather than measured (province net of the six wholly-QC CMAs, with the
Gatineau-stays-inside definition load-bearing per P2), and it must not be `borrowed_prior`, because
nothing was borrowed. The mint is deliberate and loud rather than a silent widening, and the
discriminator against the other two vocabularies goes in the module docstring in `constants.py`'s
style: an anchor flag says where a CONSTANT came from, a §7 member says what an output ROW means,
these say how THIS GEOGRAPHY'S INPUT was obtained. P8's own DECISION-SOURCE tokens already draw this
exact three-way split (DIRECT-cited / MEASURED-cited / COMPUTED-residual) — the ruling makes the
note's distinction typed rather than inventing one.

Floor-gate coverage is encoded honestly: it binds at MTL_RMR and QC_RMR only, and records
NOT-COVERED at HORS_RMR/RA06/RA13, where P8 searched all 63 sibling geography members and found no
publishable floor. No stand-in.

## Six consolidated carries

Promised to this task across three run records; they land here or they are lost. Delete
`immigrant_ratio_center` from both dicts in ONE edit (the keyset test binds them); fix `constants.py`'s
stale "only key read from this dict" comment; DELETE `formation.py`'s i2 forward-reference caveat,
since this task landing is what makes the sentence true; close the unguarded immigrant leg (negative
arrivals currently yield negative owner households); add the missing cohort/demand → loaders
import-direction gate; and make the `immigrant_ownership_ratio_fresh_arrival` docstring pointer that
amendment #7 promised and nothing has yet written.

## Advisor discipline

- `ADVISOR RULING FIRED @25b-flag-vocabulary` — ruled against flag-None for HORS_RMR (a None flag
  would state "measured directly at this territory", which the tree cannot entail for a constructed
  geography), confirmed the three-member set as the note's own distinction made typed rather than a
  third invention, required the PER-FIELD shape (a borrowed flag beside a parent value that is
  itself cited must say what THIS geography holds), and supplied the four mandate specifics above:
  the split-mandate framing, the DECISION-token coupling, the consolidated carries, and honest
  floor-gate coverage.

- run id: `wf_61e77d34-10d` (dispatched 2026-08-14; task id wt920wwbh)
- outcome: (appended at run close)

---

## OUTCOME — CLOSED CLEAN: APPROVE, 1 fix round → LANDED at `90b330e`

5 agents, 0 errors. Suites 191 + **542**, seat-verified.

**The citation chain closes at both links.** The reviewer killed all 10 table literals AND all 10 of
P8's DECISION digits as mutants, so "a regenerated note that moves a value reds the join table" is a
CLOSED property, not a sampled one. It further verified the `cited` claim for RA06/RA13 is
transitively coupled rather than decorative: `run_p8.py` calls the amendment-#11 territory guard
BEFORE composing the note, so a failing gate cannot emit the `PASS` string the join table reads. And
`test_probe_p8.py` couples P8 to spec §6 — so §6 → P8 → join table is a chain, not three independent
transcriptions of the same numbers.

**Mutation census: 58 mutants, 2 survivors**, both correctly SCOPED rather than waved off — (i)
comma-list and `__import__` import spellings that survive the layering grep, which are PEP8-forbidden
one-liners absent from the repo and outside the docstring's enumerated claim; (ii) an RA-proxy
literal re-typed equal to today's parent value, enforced transitively — it reds the moment the parent
moves, which is exactly when it matters. Killed: every provenance flip, every floor mutation, every
i2 guard, the silent-default resolve, all six carry reverts, and absolute-form layering violations in
both model trees.

**All six carries landed**, including the two that close claims rather than code: `formation.py`'s i2
forward-reference caveat is DELETED (this task landing is what made the sentence true), and
`constants.py`'s stale "only key read from this dict" now matches the tree.

### Seat error at commit, owned

The commit message first stated suites 191+**536** against my own measured 191+**542**. Caught on
re-read and amended before push, with the correction recorded in the message itself. Worth naming
rather than quietly fixing: this session has spent itself on precisely that class — a figure in
durable text that the author's own run contradicts — and the seat produced one on the last commit of
the arc's longest night. The lesson generalizes in the direction the arc already knows: it is not
knowing the rule that prevents this, it is a check that runs.

### Carries

- **PRIOR WORK, real and outside this diff:** `p_imm(-0.5, -0.5) == 0.25` and
  `immigrant_formation(100.0, 0.5, p_nonimm=-0.5, ratio=-0.5) == 12.5`, neither raising — so
  `formation.py:210-214`'s claim that "a negative or non-finite input cannot pass the product's own
  [0,1] assertion" is FALSIFIED for a negative PAIR (a single negative and any non-finite input do
  raise, verified). Unreachable through the wired path today — `p_nonimm` arrives from
  fraction-asserted ownership rates and `ratio` from the nonneg-asserted join table — so it is a
  defensive-guard gap plus a false docstring, not a live bug. **Route to Task 26** with the
  reviewer's own close: `assert_fraction` on `p_nonimm` and `assert_nonneg_finite` on `ratio` BEFORE
  the product, and the docstring narrowed to what the code does.
- **Stale spec line pointers**, inherited not introduced: `spec:352-353` and `spec:434` in the new
  module docstring now point at ruling T's table and ruling R's paragraph — the amendments grew §6 by
  ~200 lines, and the enums live at 551 and 632. The pointer came verbatim from `constants.py:37-38`
  and repeats at `test_constants.py:136-137,300-301` and `test_i2.py:317-318`, so a fix spans four
  sites, three of them committed work. The enum CONTENTS quoted are correct.
- **Task 29 wiring point:** with `immigrant_ratio_center` deleted, `assumptions_hash()` covers no
  immigrant-ratio selection, so a uniform join-table override would move the run's numbers while the
  hash stays byte-identical — the exact hazard `CENTRAL_ASSUMPTIONS`' own comment names. Nothing in
  25b emits an artifact, so no identity is wrong today; T29 is where it binds.
- **Forward interaction, fail-closed (good):** if T29 composes the arrival operand as
  `immigrants_permanents + npr_net_flow`, the new `assert_nonneg_finite` refuses LOUDLY rather than
  silently subtracting owner demand (`compo.py` records MTL_RMR 2025 `npr_net_flow` = −59,399). The
  guard makes the modeling question visible at the call site instead of inside the arithmetic.
- `FLOOR_NOT_COVERED`'s reason prose restates P8 §5 counts ("all 63 members", "0 matches") without
  digit binding; the coverage SET is fail-closed through `LIVING_ALONE_FLOOR`'s keyset, so a floor
  appearing or disappearing REDs — only the prose counts could go stale. Same shape for the
  per-geography `source` strings' embedded counts.

- outcome: **APPROVE / landed `90b330e` / suites 191+542.** Task 25b DONE. Next: Task 26 (+ the
  NARROWED QFE debt — adequacy review, not direction-measurement — plus the `p_imm` guard above),
  then 27/28 (five-input tripwire redesign), 29/30, the 31–33 pre-PR gates, and the Tranche-1 PR.
