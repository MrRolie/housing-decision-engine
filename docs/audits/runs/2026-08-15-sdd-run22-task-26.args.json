{
 "context": "Repo: housing-decision-engine, worktree /home/mm-mike/ai_system/projects/housing-decision-engine.demoflow-t1, branch feat/demoflow-tranche1 (demoflow Tranche-1 build; seat-run).\n\nGOVERNING FILES (read before implementing; they rule):\n- docs/specs/2026-07-21-demoflow-demographic-scenario-module-design.md — the LOCKED spec. docs/specs/** and docs/research/** are NO-TOUCH; a measurement contradicting a spec premise is fork-class (SEAT_QUESTION), never an edit. §7 carries the ED and OwnerStock equations (lines ~588-600).\n- docs/plans/2026-07-21-demoflow-tranche1.md — the ruled plan; this task is its Task 26 verbatim, followed by seat carries. **Plan TEST BODIES did not get the spec's review depth — treat plan code as reviewable, not gospel; where a body contradicts the spec or the tree's own resolved discipline, say so and report the divergence.** Carry 1 below is exactly that case and is already adjudicated.\n- demoflow/ is its own uv project (own venv, path dep on ../../actuarial-system which is READ-ONLY). Run demoflow tests from demoflow/. Canonical gate: `./scripts/test-all.sh` from the worktree root. Green at dispatch, seat-verified: hde 191 + demoflow 542, tree CLEAN at HEAD 22ff635.\n- Useful precedent, already committed: `demand/formation.py` resolved the identical missing-rate question for the same two curves (see `_households`, `_require_prior_cohort`, `_ownership`, `OWNERSHIP_LATTICE_FLOOR`); `demand/immigrant_inputs.py` (Task 25b) shows the house style for a guarded table.\n\nFOOTPRINT: mutate only demoflow/**. AGENTS.md, docs/** and everything else is read-only reference.",
 "worktree": "/home/mm-mike/ai_system/projects/housing-decision-engine.demoflow-t1",
 "branch": "feat/demoflow-tranche1",
 "model_impl": "opus",
 "model_review": "opus",
 "tasks": [
  {
   "title": "Task 26 — OwnerStock defining equation + excess-demand fraction + hand-worked fixture",
   "instructions": "### Task 26: OwnerStock defining equation + excess-demand fraction + hand-worked fixture\n\n**Files:**\n- Create: `demoflow/src/demoflow/balance/__init__.py`\n- Create: `demoflow/src/demoflow/balance/owner_stock.py`\n- Create: `demoflow/src/demoflow/balance/excess_demand.py`\n- Test: `demoflow/tests/test_owner_stock.py`\n- Test: `demoflow/tests/test_excess_demand.py`\n\nThe ED denominator has ONE defining equation (spec §7 codex r3-F3):\n`OwnerStock(g,t,s) = Σ_all_ages pop(a,g,t,s) × headship(a) × ownership(a)` — annual re-estimation\nfrom ISQ scenario population with BASE-YEAR Census headship + ownership held constant (PIT-fixed,\nlabeled assumption). This is a stock LEVEL estimate; ISQ-embedded mortality is correct here and\ndoes not conflict with I1 (which governs the 75+ exit FLOW model only).\n\n- [ ] **Step 1a: Write the OwnerStock failing test**\n\n`demoflow/tests/test_owner_stock.py`:\n```python\nimport pytest\n\nfrom demoflow.balance.owner_stock import owner_stock\n\n\ndef test_owner_stock_all_age_pit_fixed_equation():\n    # OwnerStock = Σ_a pop(a) * headship(a) * ownership(a)\n    # 1000*0.6*0.65 + 500*0.7*0.60 = 390 + 210 = 600.\n    s = owner_stock(\n        pop_by_age={70: 1000.0, 80: 500.0},\n        headship_by_age={70: 0.6, 80: 0.7},\n        ownership_by_age={70: 0.65, 80: 0.60},\n    )\n    assert s == pytest.approx(600.0)\n\n\ndef test_owner_stock_ignores_ages_without_rates():\n    s = owner_stock(pop_by_age={40: 1000.0}, headship_by_age={}, ownership_by_age={})\n    assert s == 0.0\n```\n\n- [ ] **Step 1b: Write the excess-demand failing test**\n\n`demoflow/tests/test_excess_demand.py`:\n```python\nimport pytest\n\nfrom demoflow.balance.excess_demand import excess_demand, MIN_OWNER_STOCK\nfrom demoflow.cohort.listings import market_listings\nfrom demoflow.demand.formation import total_owner_demand\nfrom demoflow.errors import CalibrationError\n\n\ndef test_hand_worked_ed_with_estate_lag_boundary_crossing():\n    # ED(g,t,s) = [D - S] / OwnerStock, all annual, household-denominated (spec §7).\n    # D = native 200 + immigrant 50 = 250.\n    # S = voluntary(2035)=40*0.9=36  +  estate(2034)=100 lists in 2035 at 0.75 = 75  => 111.\n    #     (estate-lag boundary crossing: 2034 death -> 2035 listing; explicit non-central params).\n    # OwnerStock(2035) = 5000.  ED = (250 - 111) / 5000 = 139/5000 = 0.0278.\n    D = total_owner_demand(native=200.0, immigrant=50.0)\n    S = market_listings(voluntary_by_year={2035: 40.0}, estate_by_year={2034: 100.0},\n                        lag=1, eventual_fraction=0.75)[2035]\n    assert S == pytest.approx(111.0)\n    ed = excess_demand(D=D, S=S, owner_stock=5000.0)\n    assert ed == pytest.approx(0.0278, abs=1e-6)\n\n\ndef test_owner_stock_numeric_boundary_999_1000_1001():\n    # codex r9-F5: OwnerStock < 1,000 households raises (never leave \"near-zero\" to taste).\n    assert MIN_OWNER_STOCK == 1000.0\n    with pytest.raises(CalibrationError, match=\"1000|OwnerStock\"):\n        excess_demand(D=10.0, S=5.0, owner_stock=999.0)\n    excess_demand(D=10.0, S=5.0, owner_stock=1000.0)    # boundary: allowed (>= 1000)\n    excess_demand(D=10.0, S=5.0, owner_stock=1001.0)\n```\n\n- [ ] **Step 2: Run to verify both fail**\n\nRun: `cd demoflow && uv run pytest tests/test_owner_stock.py tests/test_excess_demand.py -v`\nExpected: FAIL (`ModuleNotFoundError: demoflow.balance.owner_stock` / `.excess_demand`).\n\n- [ ] **Step 3: Write the implementations**\n\n`demoflow/src/demoflow/balance/__init__.py`: (empty file)\n\n`demoflow/src/demoflow/balance/owner_stock.py`:\n```python\n\"\"\"OwnerStock defining equation (spec §7, codex r3-F3). ONE equation for the ED\ndenominator: annual re-estimation from ISQ scenario population with BASE-YEAR Census\nheadship + ownership held constant (PIT-fixed). Stock LEVEL — ISQ-embedded mortality\nis correct here; I1 governs only the 75+ exit FLOW model.\"\"\"\n\n\ndef owner_stock(pop_by_age: dict[int, float], headship_by_age: dict[int, float],\n                ownership_by_age: dict[int, float]) -> float:\n    return sum(\n        pop * headship_by_age.get(a, 0.0) * ownership_by_age.get(a, 0.0)\n        for a, pop in pop_by_age.items()\n    )\n```\n\n`demoflow/src/demoflow/balance/excess_demand.py`:\n```python\n\"\"\"Excess-demand fraction (spec §7, codex F4). All terms annual, household-\ndenominated, per (geography g, year t, scenario s):\n\n    ED(g,t,s) = [ D(g,t,s) - S(g,t,s) ] / OwnerStock(g,t,s)\n\n    D = native owner-household formation + immigrant-cohort formation   (demand/formation.py)\n    S = sum_cause exits(cause) * phi_market(cause), estate lagged L      (cohort/listings.py)\n    OwnerStock = the §7 defining equation                               (balance/owner_stock.py)\n\nED is scale-invariant (households/households). Denominator guard has a NUMERIC boundary\n(codex r9-F5): OwnerStock < 1,000 households -> raise (no modeled geography legitimately\ncarries fewer; never leave \"near-zero\" to implementation taste, never emit an unbounded\nfraction). Tranche 1 stops at the raw fraction; the ED->drift mapping (beta) is Tranche 2.\"\"\"\nfrom demoflow.errors import CalibrationError\n\nMIN_OWNER_STOCK = 1000.0\n\n\ndef excess_demand(D: float, S: float, owner_stock: float) -> float:\n    if owner_stock < MIN_OWNER_STOCK:\n        raise CalibrationError(\n            f\"OwnerStock {owner_stock} < {MIN_OWNER_STOCK} households — no modeled geography \"\n            f\"legitimately carries fewer (denominator guard)\")\n    return (D - S) / owner_stock\n```\n\n- [ ] **Step 4: Run to verify they pass**\n\nRun: `cd demoflow && uv run pytest tests/test_owner_stock.py tests/test_excess_demand.py -v`\nExpected: 4 PASS.\n\n- [ ] **Step 5: Commit**\n\n```bash\ngit add demoflow/src/demoflow/balance/__init__.py demoflow/src/demoflow/balance/owner_stock.py demoflow/src/demoflow/balance/excess_demand.py demoflow/tests/test_owner_stock.py demoflow/tests/test_excess_demand.py\ngit commit -m \"feat(demoflow): OwnerStock defining equation + excess-demand fraction + <1000 guard (§7 r3-F3/r9-F5, codex F4)\"\n```\n\n---\n\n---\n\n## BINDING CARRIES (seat-authored 2026-08-15; they override the task body where they conflict)\n\n**1. THE `.get(a, 0.0)` IN `owner_stock` IS THE DEFECT THIS ARC ALREADY RULED ON — and its test body pins it.** The plan reads BOTH rate curves with `.get(a, 0.0)`, and `test_owner_stock_ignores_ages_without_rates` asserts that age 40 with population and EMPTY rate dicts returns 0.0. That is the \"missing is not zero\" class the Task-25 review raised as a HIGH finding, and `demand/formation.py` already resolved it for the same two curves. Here the direction is WORSE, and the mandate states it so the implementer can see the stake: **OwnerStock is the ED DENOMINATOR, so a silently-shrunk stock INFLATES excess demand — the very quantity the rankings rank on.** Demand shrinking quietly is bad; the denominator shrinking quietly moves every ranking row in the optimistic direction.\n\nImplement the SAME resolved discipline as `formation.py`, and report the divergence from the plan:\n- **ownership at an age BELOW the Census lattice floor (25): contributes ZERO**, by the documented upstream convention (`census.py`'s `zero_support_note` — 98-10-0231-01 publishes no owner-maintainer rate below 25). That is not a hole and must not raise.\n- **ownership ABSENT at an age AT OR ABOVE the floor, where population is nonzero: RAISE.** \"Undefined below the floor\" does not explain it; the curve is holed.\n- **headship ABSENT at any age where population is nonzero: RAISE.** The committed curve is banded across 0-100 and covers every age, so an absent headship is a hole, not a convention.\n- Where population is ZERO the term is zero whatever the rates are, so demand no rate — same carve-out `_households` already makes.\n- **Do NOT duplicate the floor constant a third time.** `formation.py` pins its copy equal to `min(census._AGE_BANDS)` by test rather than importing the Census band table; do the same, or import from `formation` if that reads cleaner — and say which and why.\n\n**RE-DERIVE the plan's superseded test body.** `test_owner_stock_ignores_ages_without_rates` as written asserts the defect. Replace it with both arms: an age at/above the floor with population and no rate must RAISE (naming the age and the floor), and a SUB-FLOOR age must still contribute zero — so the convention is pinned as a convention and the hole is pinned as a hole. The plan's first test (`test_owner_stock_all_age_pit_fixed_equation`, 600.0) is sound and stays verbatim; note that its ages 70/80 carry both rates, which is why it never exercised the door.\n\n**2. PRIOR-WORK CARRY — the `p_imm` negative-pair gap (run-21 review, outside that diff).** Measured live: `p_imm(-0.5, -0.5) == 0.25` and `immigrant_formation(100.0, 0.5, p_nonimm=-0.5, ratio=-0.5) == 12.5`, neither raising — so `demand/formation.py:210-214`'s claim that \"a negative or non-finite input cannot pass the product's own [0,1] assertion\" is FALSIFIED for a negative PAIR (a single negative, and any non-finite input, DO raise — verified). It is unreachable through the wired path today (`p_nonimm` arrives from fraction-asserted ownership rates, `ratio` from the nonneg-asserted join table), so this is a defensive-guard gap plus a false docstring, not a live bug. Close it the way the reviewer named: `assert_fraction` on `p_nonimm` and `assert_nonneg_finite` on `ratio` BEFORE the product, and narrow the docstring sentence to what the code then does. Pin the pair case by test.\n\n**3. SCOPE — Tranche 1 stops at the RAW FRACTION.** The ED→drift mapping (β) is Tranche 2 and is GATED on the S4b input-slot sketch: do not build it, do not scaffold toward it, do not add a parameter \"for later\".\n\n**4. `MIN_OWNER_STOCK` numeric boundary stays exactly as the plan writes it** (codex r9-F5): 999 raises, 1000 and 1001 pass. The plan's boundary triple is a good test and stays verbatim.\n\nSEAT-RUN ADDENDUM (binding): commit NOTHING — the seat commits at run close. RED-first binds on every new test. Run-unique scratchpad subdir; PYTHONDONTWRITEBYTECODE=1 for mutation batteries. Finish with `./scripts/test-all.sh` from the worktree root; real tail.",
   "money_path": false,
   "load_bearing": true
  }
 ]
}
