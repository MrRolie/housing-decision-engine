# Seat-run dispatch record — run 22 (Task 26: OwnerStock + excess-demand fraction)

First production math of the arc — the ED denominator and the excess-demand fraction the rankings
will rank on. The standing QFE re-triage debt is attached to this task and fires at the fold.

- date: 2026-08-15
- script: `mm-spine/.claude/workflows/mm-sdd-pipeline.js` sha256
  `d7743e42b0ebe6499f8f5af986b471ea3417aff067710048dc39aa8756e315ea` @ mm-spine `f4d7cca`
- args: `2026-08-15-sdd-run22-task-26.args.json` (sha256
  `61a171a99a6b9fe73ea0171be223439844c40117c68414a763fee1662fd1f2b7`); plan Task 26 bytes extracted
  MECHANICALLY (lines 3740→Task 27), followed by four seat carries
- models: opus/opus; load_bearing ×1; money_path: false; WAVE-0 vacuous
- preconditions (GROUND FIRST, seat's own run): tree CLEAN at HEAD `22ff635`, in sync with origin,
  `./scripts/test-all.sh` green at hde 191 + demoflow **542**; checkout free (session-telemetry,
  scope=all — no session on either worktree)

## Pre-dispatch catch: the plan's `owner_stock` re-opens a ruled defect, and its test pins it

The plan reads BOTH rate curves with `.get(a, 0.0)` and asserts, in
`test_owner_stock_ignores_ages_without_rates`, that age 40 with population and EMPTY rate dicts
returns 0.0. That is the "missing is not zero" class the Task-25 review raised as a HIGH finding and
that `demand/formation.py` has already resolved for the same two curves.

**Here the direction is worse.** OwnerStock is the ED DENOMINATOR: a silently-shrunk stock INFLATES
excess demand, which is the quantity Task 27 ranks geographies on. Demand shrinking quietly moves one
term; the denominator shrinking quietly moves every ranking row in the optimistic direction.

The spec does not rule it either way — §7:593 says only `Σ_over_all_ages pop × headship × ownership`
— so this is a plan-body question the tree has already answered elsewhere, and the mandate directs
consistency rather than a fresh judgment: ownership absent BELOW the Census lattice floor contributes
zero (the documented `zero_support_note` convention, not a hole); ownership absent AT OR ABOVE the
floor with nonzero population RAISES; headship absent at any age with nonzero population RAISES (the
committed curve is banded across 0-100 and covers every age); zero population demands no rate. The
plan's superseded test is re-derived with BOTH arms — the hole pinned as a hole, the convention pinned
as a convention — and the floor constant is not duplicated a third time.

## Other carries

- **Prior-work gap from run 21's review:** `p_imm(-0.5, -0.5) == 0.25` without raising, falsifying
  `formation.py:210-214`'s claim that a negative input cannot pass the product's [0,1] assertion (a
  single negative, and any non-finite, DO raise). Unreachable through the wired path today, so a
  defensive gap plus a false docstring. Closed with `assert_fraction(p_nonimm)` +
  `assert_nonneg_finite(ratio)` before the product, docstring narrowed, pair case pinned.
- Tranche-1 scope restated: stop at the RAW fraction; the ED→drift β mapping is Tranche 2 and gated
  on the S4b input-slot sketch — not to be built, scaffolded, or parameterised "for later".
- `MIN_OWNER_STOCK`'s 999/1000/1001 boundary triple stays verbatim; it is a good test.

## SEAT DISPATCHES AT THIS RUN'S FOLD — the QFE re-triage debt (standing since run 5)

`mm-spine:quant-financial-engineer`, on the first production math. **NARROWED by amendments #9/#11:**
the METRIC direction it was originally chartered to measure is now MEASURED (person-weighted
OVERSTATES the maintainer-denominated ratio: MTL −0.5%, QC −3.4%), and both immigrant inputs are
measured at zero geography/metric transport — so its charge is **adequacy review of ruled inputs, not
direction-measurement**. Its named subject remains the **HORS_RMR rate-territory proxy**: the
ownership rate is estimated over province-minus-six-CMAs (which INCLUDES the Gatineau QC side) while
multiplying the ISQ hors-RMR population (which EXCLUDES it) — a 355,971-person gap, 12.99% of the
Census residual / 14.93% of ISQ hors-RMR. Under rulings S/T the SAME residual construction now also
produces HORS_RMR's immigrant headship and ratio, so the QFE evaluates the proxy across both
surfaces. It gets a verdict, not an inheritance.

## Advisor discipline

- `ADVISOR RULING SKIPPED @run-22 dispatch — no new ruling. The one adjudication (carry 1) cites the
  tree's own resolved discipline by symbol — demand/formation.py's `_households` / `_ownership` /
  `OWNERSHIP_LATTICE_FLOOR` and census.py's `zero_support_note` — and the standing plan-tests-are-
  reviewable rule from the charter. No cap value, gate semantics or order-path direction is set.`

- run id: `wf_177ed165-f63` (dispatched 2026-08-15; task id wbzz68vo5)
- outcome: (appended at run close)
