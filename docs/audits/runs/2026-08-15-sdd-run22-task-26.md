# Seat-run dispatch record — run 22 (Task 26: OwnerStock + excess-demand fraction)

First production math of the arc — the ED denominator and the excess-demand fraction the rankings
will rank on. The standing QFE re-triage debt is attached to this task and fires at the fold.

- date: 2026-08-15
- script: `mm-spine/.claude/workflows/mm-sdd-pipeline.js` sha256
  `d7743e42b0ebe6499f8f5af986b471ea3417aff067710048dc39aa8756e315ea` @ mm-spine `f4d7cca`
- args: `2026-08-15-sdd-run22-task-26.args.json` (sha256
  `61a171a99a6b9fe73ea0171be223439844c40117c68414a763fee1662fd1f2b7`); plan Task 26 bytes extracted
  MECHANICALLY (lines 3740→Task 27), followed by four seat carries
- models: opus/opus; load_bearing ×1; money_path: false; WAVE-0 vacuous
- preconditions (GROUND FIRST, seat's own run): tree CLEAN at HEAD `22ff635`, in sync with origin,
  `./scripts/test-all.sh` green at hde 191 + demoflow **542**; checkout free (session-telemetry,
  scope=all — no session on either worktree)

## Pre-dispatch catch: the plan's `owner_stock` re-opens a ruled defect, and its test pins it

The plan reads BOTH rate curves with `.get(a, 0.0)` and asserts, in
`test_owner_stock_ignores_ages_without_rates`, that age 40 with population and EMPTY rate dicts
returns 0.0. That is the "missing is not zero" class the Task-25 review raised as a HIGH finding and
that `demand/formation.py` has already resolved for the same two curves.

**Here the direction is worse.** OwnerStock is the ED DENOMINATOR: a silently-shrunk stock INFLATES
excess demand, which is the quantity Task 27 ranks geographies on. Demand shrinking quietly moves one
term; the denominator shrinking quietly moves every ranking row in the optimistic direction.

The spec does not rule it either way — §7:593 says only `Σ_over_all_ages pop × headship × ownership`
— so this is a plan-body question the tree has already answered elsewhere, and the mandate directs
consistency rather than a fresh judgment: ownership absent BELOW the Census lattice floor contributes
zero (the documented `zero_support_note` convention, not a hole); ownership absent AT OR ABOVE the
floor with nonzero population RAISES; headship absent at any age with nonzero population RAISES (the
committed curve is banded across 0-100 and covers every age); zero population demands no rate. The
plan's superseded test is re-derived with BOTH arms — the hole pinned as a hole, the convention pinned
as a convention — and the floor constant is not duplicated a third time.

## Other carries

- **Prior-work gap from run 21's review:** `p_imm(-0.5, -0.5) == 0.25` without raising, falsifying
  `formation.py:210-214`'s claim that a negative input cannot pass the product's [0,1] assertion (a
  single negative, and any non-finite, DO raise). Unreachable through the wired path today, so a
  defensive gap plus a false docstring. Closed with `assert_fraction(p_nonimm)` +
  `assert_nonneg_finite(ratio)` before the product, docstring narrowed, pair case pinned.
- Tranche-1 scope restated: stop at the RAW fraction; the ED→drift β mapping is Tranche 2 and gated
  on the S4b input-slot sketch — not to be built, scaffolded, or parameterised "for later".
- `MIN_OWNER_STOCK`'s 999/1000/1001 boundary triple stays verbatim; it is a good test.

## SEAT DISPATCHES AT THIS RUN'S FOLD — the QFE re-triage debt (standing since run 5)

`mm-spine:quant-financial-engineer`, on the first production math. **NARROWED by amendments #9/#11:**
the METRIC direction it was originally chartered to measure is now MEASURED (person-weighted
OVERSTATES the maintainer-denominated ratio: MTL −0.5%, QC −3.4%), and both immigrant inputs are
measured at zero geography/metric transport — so its charge is **adequacy review of ruled inputs, not
direction-measurement**. Its named subject remains the **HORS_RMR rate-territory proxy**: the
ownership rate is estimated over province-minus-six-CMAs (which INCLUDES the Gatineau QC side) while
multiplying the ISQ hors-RMR population (which EXCLUDES it) — a 355,971-person gap, 12.99% of the
Census residual / 14.93% of ISQ hors-RMR. Under rulings S/T the SAME residual construction now also
produces HORS_RMR's immigrant headship and ratio, so the QFE evaluates the proxy across both
surfaces. It gets a verdict, not an inheritance.

## Advisor discipline

- `ADVISOR RULING SKIPPED @run-22 dispatch — no new ruling. The one adjudication (carry 1) cites the
  tree's own resolved discipline by symbol — demand/formation.py's `_households` / `_ownership` /
  `OWNERSHIP_LATTICE_FLOOR` and census.py's `zero_support_note` — and the standing plan-tests-are-
  reviewable rule from the charter. No cap value, gate semantics or order-path direction is set.`

- run id: `wf_177ed165-f63` (dispatched 2026-08-15; task id wbzz68vo5)
- outcome: (appended at run close)

---

## OUTCOME — CLOSED CLEAN: APPROVE, 2 fix rounds → LANDED at `0ad4ac6`

7 agents, 0 errors. Suites 191 + **604**, seat-verified (figure derived from the run).

**THE PRE-DISPATCH CATCH IS QUANTIFIED.** The review measured the plan's own body on scratchpad
replicas: `owner_stock` with `.get(a, 0.0)` returns **390.0 where the tree now raises and the correct
dense value is 780.0** — a HALVED denominator, ED inflated 2×, every intermediate a plausible float.
It also returns 0.0 for the empty-dict and 24-vs-25 cases, `nan` for a NaN population cell, −390.0 for
a negative one, and 780.0 for a headship of 1.2. The plan's `test_owner_stock_ignores_ages_without_
rates` pinned that behaviour, so **the defect would have shipped under a green suite** — which is the
whole argument for reading plan bodies as reviewable rather than gospel.

Carry compliance verified rather than accepted: the `.get` is gone and the superseded test is absent
from the tree; both arms are pinned (hole at/above the floor raises naming age 40 and floor 25; all
25 sub-floor ages contribute zero AND a supplied sub-floor rate is still used); the floor is IMPORTED
from `demand.formation` with the chain pinned to `census._AGE_BANDS`. Carry 2 (`p_imm`) closed with
the r7-F8 ratio>1 carve-out intact and the false sentence retired. Carry 3 grep-clean: no β, no
mapping module, no parameter "for later". Carry 4's boundary triple byte-verbatim.

**A DELIBERATE DIVERGENCE, argued rather than asserted:** population cells are asserted nonneg-finite
in `owner_stock`, unlike `formation`'s "P_resident ≥ 0 is asserted upstream" stance. The positional
reason is in the docstring — NaN does not vanish from a SUM the way it can from a gain comparison, and
`NaN < MIN_OWNER_STOCK` is False, so the r9-F5 guard would pass a NaN straight through to the emitter.

**False-red risk closed STRUCTURALLY, not by sampling:** `isq_ages.py:169` enforces the population
lattice as exactly 0..100, both band specs top out at hi=200, and a read-only live probe over **72
slices** (8 modeled geographies × 3 fans × {first, mid, terminal} year) raised zero times with the age
domain 0..100 in every slice.

### The run's own new finding — the sub-floor convention priced on the DENOMINATOR side

`formation.py` had priced the ownership-lattice floor's cost on the DEMAND side; nobody had priced it
on the OwnerStock side, where the direction is the one that module's docstring calls the worst in the
tree. Upper bound, each geography against its own 25-54 rate: MTL_RMR **5.90%** (2021) / 5.02%
(2051), QC_RMR 5.40% / 5.30%, LAVAL_RA13 5.66% / 4.72% — **OwnerStock understated, so ED overstated,
i.e. optimistic on the ranked axis.** Doubly loose (real sub-25 maintainer ownership sits well below
the 25-54 rate, and the 20-24 slice inherits the 20-34 BANDED headship the zero_support_note warns
against) and near-uniform across geographies, so it moves LEVELS more than RANKINGS. The convention
itself is upstream-ruled and nothing here contradicts it. **Routed to the QFE as subject 2.**

### Carries

- `excess_demand` with a non-numeric D, S or owner_stock leaks `TypeError` from `math.isfinite`
  rather than `CalibrationError` — unreachable today (both numerator terms arrive from float
  arithmetic) and consistent with the module's deliberate validate-free pure-leaf stance, which the
  reviewer confirmed (`import demoflow.balance.excess_demand` pulls no pandas; `owner_stock` does).
- The −inf case of the non-finite-OwnerStock test does not discriminate against the plan body
  (−inf < 1000 is already True); the nan and +inf cases carry that guard.
- `test_the_r9_F5_guard_CANNOT_see_a_non_finite_denominator_on_its_own` asserts Python comparison
  semantics and never calls `excess_demand`; its stated purpose is achieved by the parametrized
  behaviour test below it. Its claim is true and correctly demonstrated — it is just not the
  instrument.
- Implementer-report precision (artifact right, report imprecise): "smallest stock anywhere
  232,673.5" came from a sample fixing the year at 2051 for 7 of 8 geographies; over the 72-slice
  sweep the true minimum is **99,692.3** (LAVAL_RA13 / REFERENCE / 2021) — still ~100× above
  MIN_OWNER_STOCK, so r9-F5's premise is corroborated either way.
- Class census enumerates pinned vs merely-covered siblings for every input surface; the unpinned
  set (−inf population, non-numeric cells, NaN/+inf headship, <0/+inf ownership, D −inf with S +inf)
  is recorded rather than closed.

### Seat error at commit, owned — the SECOND in two commits, so the procedure changed

The commit message first stated suites 191+**601** — the reviewer's count, taken on faith — against
my own measured 191+**604**. Amended before push with the correction in the message. Two instances in
two commits is not carelessness to be resolved by more care: the figure is now COMPUTED into the
commit message from a live run rather than transcribed. A check that runs beats a rule that is known.

## QFE RE-TRIAGE DEBT — DISCHARGED AT THIS FOLD (dispatched, seat-side)

`mm-spine:quant-financial-engineer`, read-only, verdict-not-inheritance, three subjects: (1) the
HORS_RMR rate-territory proxy, now spanning BOTH the ownership surface and — under rulings S/T's
identical residual construction — the immigrant headship and ratio, with the question of whether the
errors compound, offset, or are independent; (2) the sub-floor convention priced on the denominator
side above, asking whether near-uniformity is enough to protect a rankings-and-tripwires deliverable
and what would falsify it; (3) adequacy of the `Before 2016` settled member for a model that credits
each arrival cohort once in its arrival year, with the METRIC direction explicitly OUT of scope
because it is already measured. Plus the ED equation's dimensional composition (annual FLOWS over a
STOCK level) and whether a guard ~100× below any real value is useless or correctly structural.

- outcome: **APPROVE / landed `0ad4ac6` / suites 191+604.** Task 26 DONE. Next: 27/28 (rankings +
  the five-input tripwire redesign), then 29/30, the 31–33 pre-PR gates, and the Tranche-1 PR.
