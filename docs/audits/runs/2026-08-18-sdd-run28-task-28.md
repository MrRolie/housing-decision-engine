# Seat-run dispatch record — run 28 (Task 28: tripwire baselines)

- date: 2026-08-18
- script: `mm-spine/.claude/workflows/mm-sdd-pipeline.js` sha256
  `d7743e42b0ebe6499f8f5af986b471ea3417aff067710048dc39aa8756e315ea` @ mm-spine `f4d7cca`
- args: `2026-08-18-sdd-run28-task-28.args.json` (sha256
  `7504ed1f9f004a8f9841cc231aa91b1a8cbf6ecc33f07113f6d624fa907945a7`); plan Task 28 bytes extracted
  MECHANICALLY, followed by the five binding design inputs
- models: opus/opus; load_bearing ×1; money_path: false; WAVE-0 vacuous
- preconditions, DERIVED at authoring time: tree CLEAN at HEAD `249d5f1`, hde **191** + demoflow **782**

## This task's plan body is known-defective, and the mandate says so up front

The plan hardcodes `realized = 45000.0` — its own band midpoint — so the indicator would compare a
number to itself and pass forever. It also compares a PROVINCIAL plan constant against CMA-PAIR
reference values, two different geographies with no like-for-like ratio (the pair's provincial share
decays ~4pp/yr, so no fixed offset patches it, and the near-match 45,895 ≈ 45,000 is a coincidence of
mismatched scopes).

The five design inputs were measured at the 2026-08-08 DIV fold against the live IRCC feed and
override the body: realized is COMPUTED; the tripwire numerator is the PROVINCIAL sum while the CMA
pair stays the model's demand input; only years the plan in force governs are evaluable (first is
2026 full-year, UNKNOWN until then, never a cross-era comparison); the discriminating completeness
check is TWELVE DISTINCT MONTHS — which kills both the measured partial-year false-CROSSED (7,450 vs
45,895 in the 2025-08 vintage) and the silent interior gaps that exist for 103 of 172 members — with
the freshness limit set no tighter than ~5 months or the indicator is permanently UNKNOWN; and a
degenerate all-suppressed feed RAISES TO UNKNOWN rather than crossing (measured: −81% realized
evaluates CROSSED under presence-only checking).

Feed facts carried: tab-delimited despite `.csv`, base-5 rounding, and `'--'` absorbs the whole 0-5
band — **the feed has no zeros, so a `'--'` is not a zero.** Ruling D applies (TOTAL-by-CMA suffices;
category-by-CMA does not exist free and must never be fabricated). Task-30 input recorded because it
constrains the emission: IRCC restates history a decade deep, so the vintage (Last-Modified + content
sha) must be recorded for a red to be attributable to data-vs-code.

## Advisor discipline

- `ADVISOR RULING SKIPPED @run-28 dispatch — no new ruling. The five inputs are DIV-measured facts
  already consolidated in the charter and this arc's records; the mandate transcribes them and names
  the plan-body defects they override. No cap value or gate semantics is set here.`

- run id: `wf_f219d0d4-1de` (task `wbj3lz44v`); 3 agents, 0 errors, 458,458 subagent tokens, 63 min

## Outcome — SEAT_QUESTION, folded 2026-08-18

Landed as `43961c7` (pushed). Gate re-run BY THE SEAT, not transcribed from the reviewer:
`./scripts/test-all.sh` with `PYTHONDONTWRITEBYTECODE=1` → **hde 191 passed, demoflow 844 passed,
"both suites passed"**. Tree clean at 0 paths after commit.

Review found **8 defects, all reproduced from real bytes, 0 refuted**; 7 fixed in-lane (F2–F8 plus
one self-caught and two false WHYs corrected), mutation battery 14/14 KILLED. The reviewer also
VERIFIED THE VINTAGE LIVE rather than accepting it: independent GET, 1,727,985 bytes, sha256
`d5af3237…a444a14b`, and all 560 fixture rows found verbatim in the live bytes.

**One dispatch-record premise was REFUTED by measurement, and it was mine.** This record states the
twelve-distinct-months check kills "the silent interior gaps that exist for 103 of 172 members". It
does not. Province-wide month presence is a UNION over all members: real 2025 shows 12 distinct
province months with **11 of 32 Quebec members carrying interior gaps** (Hawkesbury missing 9,
Lachute 4, Sainte-Marie 3). The gate's own code comment carried the same false why and has been
rewritten. Recorded here because a dispatch premise that survives into a run record becomes the next
mandate's inherited fact.

**Seat catch at commit, outside the review's scope.** `.gitattributes` `* text=auto` normalized the
fixture's CRLF to LF in the index — 40,669 → 40,108 bytes, all 561 CR bytes stripped. The fixture's
entire evidentiary value is that it IS the feed's bytes, and a fresh checkout would have exercised
line endings the feed never sends while the verified worktree copy kept CRLF. Pinned `-text`; index
blob now byte-identical to the worktree (sha256 `7b137023…0f91ad0f`).

## RULING U (steering, 2026-08-18) — the completeness contract is MEMBER-SET, not month-presence

**The open CRITICAL.** The completeness gate verifies month presence, never member coverage. Measured
by the reviewer: truncate the feed to the two modeled members (30 of 32 Quebec members dropped) and
the gate publishes **45,895 as the PROVINCIAL realized, status OK, exit 0**, record accepted — the
CMA-pair total wearing the province's name, which is exactly the scope confusion this module's own
SCOPE block exists to prevent, re-entering through a data path instead of a literal.

**RULED: option (a), refined — a code-owned REQUIRED member set of 31, subset-checked.** Not the
reviewer's framing of "a pinned set that reds when IRCC changes CMA delineation", because the seat
measured the premise and it does not hold. Across all twelve published years (2015–2026, 3,544 Quebec
rows) the member set is **stable at exactly 31**, with exactly one intermittent member —
`Hawkesbury (Quebec part)`, present in 6 of 12 years. So the constant is the **cross-year
intersection (31), Hawkesbury optional**: the LARGEST set that clears every real year, which is what
makes it earned rather than tuned.

Measured consequences, all on the live vintage:
- **Zero regression** — the rule closes all eleven real closed years, including **2020 (provincial
  25,005, the historical minimum, all 31 members still published)**. That year is the anchor: any
  count-or-total FLOOR loose enough to admit a 50%-COVID collapse also admits a large truncation,
  which is why option (b) is dominated and why this is a set, not a threshold.
- **Kills the truncation path** — the two-member feed yields `closed_plan_years == []` → UNKNOWN,
  exit 1.
- **Kills an exploit nobody had listed.** Dropping a SINGLE non-modeled member (Saguenay) leaves the
  shipped gate closing every year and publishing 59,335 against a true 60,010. Stated precisely,
  because the seat measured it rather than assuming: at the probe band (55k, 65k) the drop **never
  flips the verdict** on any year — deltas run −60 to −675. It is a **value-integrity breach, not a
  verdict flip**, and the flip risk is band-dependent and unresolved until the production band is
  wired at Task 29. Option (c) — accept the residual — died on this measurement.

**The asymmetry is the point, and belongs in the why.** `REQUIRED ⊆ present` means a delineation
ADDITION still closes and still sums provincially, while a removal or rename reds to UNKNOWN. A
structural change in the feed is precisely when a human must look.

**NAMED RESIDUAL after this ruling, with its assumption stated.** Non-modeled members keep a
presence bar (≥1 cell in the year), not 12/12, because 11 of 32 carry interior gaps in real data.
The bar rests on an INFERENCE about IRCC publication semantics — that an unpublished month is zero
landings rather than missing data, the feed publishing `--` for 1–5 and omitting true zeros. That is
the seat's reading of the feed's behaviour, **not a documented IRCC statement**, and it is the reason
the bar is presence rather than completeness. The two MODELED members carry the stricter 12/12 rule
already shipped.

**Spec disposition.** Spec §7c pins the tripwire RECORD contract but pins NO completeness rule — the
words "twelve months" appear nowhere in it. So this ruling ADDS a contract rather than superseding
spec text, and lands as **amendment #15**, seat-authored, **in the same commit as the successor's
code** so spec and tree never disagree in between (amendment #14's standing rule).

**Not implemented in-lane.** The pipeline halts fix-stage SEAT_QUESTIONs without resume; this goes to
a successor as a brownfield mandate. The constant lands GENERATED from the feed, never hand-typed
(the arc's depth-1 ladder), carrying the vintage sha, the intersection computation, Hawkesbury's
6-of-12, and the 2020 anchor.

**Log discriminator.** The truncation state will surface as `source_unavailable` like every other
empty-closed-years cause — the enum is spec-closed and correctly stays so — therefore the run-log
line must NAME member-set truncation, or the artifact reader cannot tell a pre-era refusal from a
gutted feed.

## Advisor discipline

- `ADVISOR RULING FIRED @run-28 fold — completeness contract (ruling U).` Advisor confirmed option
  (a)-refined and dominated (b) on the 2020 anchor, and returned two precision corrections the seat
  ADOPTED: the Saguenay verdict flip was asserted without being measured (measured after: no flip at
  the probe band), and "a gap = zero landings" is a seat inference about IRCC semantics, not feed
  documentation — it now rides inside the named residual as a stated assumption rather than as fact.

## Carry to the successor and beyond

- **Province-level sibling feed — PARKED, and the absence claim is bounded.** A GET of
  `ODP-PR-PT.csv` returned 404. That is the absence of ONE GUESSED URL, not the absence of a
  province-level feed; the catalogue was not searched. Recorded because such a feed would turn
  completeness into a reconcilable IDENTITY (sum of members == published province total) instead of
  a code-owned set. The CMA file itself publishes no `Total` member, so no in-file identity exists.
- **`run_exit_code` has no production consumer** until the Task-29/30 artifact wiring. It is the gate
  that requires the evaluated indicator multiset to EQUAL the required set; if Task 29 wires
  `exit_code` instead, the coverage question goes unasked and F4 is only half-fixed.
- **The suppression envelope is centred where the arithmetic is not.** `±2.5 × n_cells` treats a
  suppressed cell's [0,5] interval as ±2.5 about a contributed 0, but suppression is ONE-SIDED — each
  `--` can only add. For 2025 that is 51 cells whose true contribution lies in [0, +255]. Not a
  defect in this task; it is an honesty question for the Task-30 artifact's stated interval.
