### 2026-07-21 · task-scoped · task-2
- tier: STANDARD
- changed_shape: money-path NO (repo CLAUDE.md `money-path: no`); load-bearing-claim YES — matched "fail-loud on drift", "checksummed", and a verdict gate asserted never to false-green; file-glob classes hit = live vendor-boundary probe (StatCan WDS REST), new committed data extract that becomes pinned truth for all downstream ownership rates, new invariant/guard code (3 test gates + a structural metadata guard). No math substrate, no CV/label/walk-forward, no ArcticDB/broker.
- candidate_reviewers: [quant-financial-engineer, stress-tester, backtest-validator, data-integrity-validator]
- fired: [stress-tester]
- fired_why: {stress-tester: "trigger-matrix row 'introduces or modifies hardening code, regression tests, or invariant checks' + explicit load-bearing phrases 'fail-loud'/'checksummed'/'never false-green' — the #437 arm, fires regardless of money-path"}
- framing: {stress-tester: "Per-task adversarial gate, STANDARD tier, neutral-core-first with author claims in a separate NON-EXHAUSTIVE section. Emphasized motions inlined: predicate-vs-claim; guard-mutation (mutate the guard AND the subject it protects AND the call site); live-population-probe; test-double-divergence. Bounded 6-edge probe set: (1) the three gates, mutate guard+subject+call-site for each, enumerate every path not one exemplar; (2) the verdict gate's SKIP branch as a false-clean vector; (3) the drift gate's regex pin extraction against a note holding three sha256 values; (4) the content gate's five invariants as proxies-vs-properties; (5) run_p2.py's 'Que' prefilter + raw-line re-emission + ragged-row raise + utf-8-sig, hunting inputs that silently produce a WRONG extract that self-pins and passes all gates; (6) the _geography_universe structural guard. Required separation of 'extract is wrong'/'note asserts something false' from all other findings."}
- not_fired: {quant-financial-engineer: "Task 2's only arithmetic is a single ratio (owner/total) computed as an oracle CHECK, not as production math; no units conversion, no variance/vol/Greeks, no sizing or ranking. RE-TRIAGE PROMISE: dispatch at Task 26 (OwnerStock defining equation + excess-demand fraction), where this extract's ownership rate first enters production math — that task carries the dimensional/units surface this one does not.", backtest-validator: "no CV splitter, no label construction, no walk-forward, no train/test split, no ML pipeline, no reported backtest metric. Genuine no-trigger-match; nothing in Tranche 1 tasks 0-30 introduces one.", data-integrity-validator: "TRIGGER GENUINELY MATCHED (new data source + PIT identity chain + pinned-truth extract) and was NOT suppressed — deferred on coverage overlap only: the stress-tester's live-population probe independently re-derived the extract row-for-row from the raw 811 MiB source with a full csv.reader (50,400 == 50,400; CSV_RECORDS 3585602 == RAW_LINE_COUNT 3585602), which is the extract-integrity surface DIV would have re-walked. RE-TRIAGE PROMISE: dispatch at Task 13 (ownership loader), the first CONSUMER read of this extract, where the un-probed DIV surface actually lives — PIT semantics of the pinned `extracted_at` vintage, schema-drift detection on re-pull, staleness policy, and downstream blast radius. This is a DEBT, not a dismissal: honor it at Task 13, do not re-defer."}
- bypass: null

### 2026-07-21 · task-scoped · task-3
- tier: STANDARD
- changed_shape: money-path NO; load-bearing-claim YES — a three-gate verdict/citation guard asserted never to false-green, PLUS an explicit defend-against-a-known-bug-class claim (the `Fact` registry claimed to make an un-computed literal IMPOSSIBLE to state as derived). File-glob classes hit = live vendor-boundary probe (StatCan WDS), new guard/invariant code, generated provenance artifact. No math substrate, no CV/label, no committed data file (values live in a note, not a data artifact).
- candidate_reviewers: [quant-financial-engineer, stress-tester, backtest-validator, data-integrity-validator]
- fired: [stress-tester]
- fired_why: {stress-tester: "trigger-matrix rows 'introduces or modifies hardening code, regression tests, or invariant checks' AND 'claims to defend against a known bug class — that claim itself is the load-bearing assertion the probe must adversarially test'. The Fact-registry impossibility claim is precisely such an assertion."}
- framing: {stress-tester: "Per-task adversarial gate, STANDARD tier, neutral-core-first with author claims in a separate NON-EXHAUSTIVE section explicitly labelled 'not a fence'. Emphasized motions inlined: predicate-vs-claim; guard-mutation (mutate the guard AND the subject AND the call site); live-population-probe. MOTIVATING FAILURE NAMED: the three-times-recurring 'generated provenance artifact states a fact its own code cannot compute, under a header claiming the artifact is derived' (P2 166-GEO-members, P2 Campbellton/Hawkesbury, P3 §5) — instructed to attack the impossibility claim DIRECTLY and to find the bypasses nobody has named, with the known identities/suppressed gap pre-disclosed as already-logged. Bounded 6-edge probe set: (1) Fact registry + provenance header, incl. whether a stale literal can be wrapped in Fact.derived(); (2) the three gates, guard+subject+call-site mutation, every path; (3) the _token parser's just-fixed greedy match; (4) the skip branch after the GET->POST fix; (5) _fetch response-keying vs the WDS ordering trap + status:FAILED cells + base-5 rounding; (6) rate-formula denominators and degenerates. Required separation of 'recorded value is WRONG' / 'note ASSERTS SOMETHING FALSE' from all else."}
- not_fired: {quant-financial-engineer: "Task 3's arithmetic is demographic rate ratios (living_alone = alone/pop; couple_share = coupled/(pop-alone)), not quant-finance math — no variance/vol/Greeks/PIT-join/sizing/ranking. The stress-tester covered denominator-correctness under predicate-vs-claim and confirmed it inverts spec line 158 exactly. RE-TRIAGE PROMISE STANDS: Task 26 (OwnerStock eq + excess-demand), the first production math.", backtest-validator: "no CV splitter, no label construction, no walk-forward, no train/test split, no ML pipeline, no reported backtest metric. Genuine no-trigger-match.", data-integrity-validator: "TRIGGER PARTIALLY MATCHED (new data source, values Task 15b consumes) and NOT suppressed — but Task 3 commits NO data artifact: the values live in a generated observation note, so there is no schema, no persisted write target, no staleness surface, and no PIT join to attack yet. The DIV surface lands where the artifact does. RE-TRIAGE PROMISE: dispatch at Task 15b (living_arrangement.json is committed and loaded) — in ADDITION to the standing Task 13 promise from the task-2 record. Both are DEBTS; honor at the named task, do not re-defer."}
- bypass: null

### 2026-07-25 · task-scoped · task-4
- tier: STANDARD
- changed_shape: money-path NO; load-bearing-claim YES — a three-gate verdict/citation guard asserted never-false-green, the spec-§6 "NO silent 1.0" ban, a NOT-FOUND-AT-CMA dimension-level audit that DRIVES the documented fallback, and computed bands feeding the coarse-netting multiplier. File-glob classes: live WDS vendor-boundary probe, new guard/invariant code, generated provenance artifact. No math substrate, no CV/label, no committed data file (the band lives in a note).
- candidate_reviewers: [quant-financial-engineer, stress-tester, backtest-validator, data-integrity-validator]
- fired: [stress-tester]
- fired_why: {stress-tester: "trigger 'introduces/modifies hardening code + invariant checks' AND 'claims to defend against a known bug class' (the never-false-green gate + the earned-NOT-FOUND fail-safe are the load-bearing assertions to attack)."}
- framing: {stress-tester: "Framed neutral-core-first (full diff + house rules + standing motions), author claims in a separate NON-EXHAUSTIVE section flagged 'not a fence'. Emphasized motions: predicate-vs-claim; guard-mutation (guard+subject+call-site); cheapest-passing-world (on the NOT-FOUND verdict + silent-1.0 ban); live-population-probe. Three MOTIVATING FAILURES named: (1) the gate must never false-green on the fallback path (shape-a hole history + shape-b citation-shape-vs-referential-truth boundary); (2) NOT-FOUND-AT-CMA must be EARNED not a give-up (attack the outage/empty/parse-fail paths); (3) the silent-1.0 ban. Bounded 5-edge probe set; b0.5 mutation-harness hazard flagged (everything committed at 428d1f0). Required separation of 'value WRONG'/'note asserts false' from all else."}
- not_fired: {quant-financial-engineer: "P4's arithmetic is ratios of published homeownership rates (demographic), not quant-finance math — no variance/vol/Greeks/PIT-join/sizing. RE-TRIAGE PROMISE STANDS: Task 26 (OwnerStock eq + excess-demand), first production math.", backtest-validator: "no CV/label/walk-forward/train-test/ML/backtest-metric. Genuine no-trigger-match.", data-integrity-validator: "P4 commits NO data artifact — the band lives in a generated note, so no schema/persisted-write-target/staleness-surface/PIT-join to attack yet. RE-TRIAGE PROMISE: dispatch at Task 15 (the multiplier constant lands committed) in addition to the standing Task 13 + Task 15b promises. DEBT, honor at the named task."}
- bypass: null
- verdict: PROCEED-WITH-MODIFICATIONS. stress-tester returned F3 [MED, reopener: §1 family enumeration omits the 37* apprentices rent-substring hit], F4 [MED, reopener-adjacent: "audited both directions" overstates coverage — 173/525 title-gated 98* cubes], F1 [HIGH: empty-200 WDS response greens as an EARNED NOT-FOUND with no floor guard — the fail-safe fires only on exception; NS#1 fail-safe-gate cardinal doctrine, propagates to P5/P5b]. All three FOLDED (final P4 fold). Carried: F2 [dead outage-skip test machinery, forward-looking for P5/P5b], A [_tenure_dims vocab narrowness], shape-a forward-looking note. Robust (verified): band arithmetic recomputes exact, silent-1.0 ban solid, 13 Facts all computed.

### 2026-07-25 · task-scoped · task-5
- tier: STANDARD
- changed_shape: money-path NO; load-bearing-claim YES — a hardened verdict/evidence gate asserted never-false-green, an earned-LOCATED floor guard (_guard_csv_shape) that must refuse a vacuous/wrong-body response, and DECISION-block tokens (FOUND-AT-CMA, CATEGORY-AT-CMA, CSV-XLSX-UNROUNDED-URL, SUPPRESSED-CONVENTION) that Task 28's tripwire parses. File-glob classes: live CKAN+IRCC vendor-boundary probe, new guard/invariant code, generated provenance artifact. No math substrate, no CV/label, no committed data file (records a url + schema contract).
- candidate_reviewers: [quant-financial-engineer, stress-tester, backtest-validator, data-integrity-validator]
- fired: [stress-tester]
- fired_why: {stress-tester: "trigger 'introduces/modifies hardening code + invariant checks' AND 'claims to defend against a known bug class' (never-false-green gate + earned-LOCATED floor guard). Note: the just-fixed 'conditional value + unconditional gloss' class in DECISION tokens IS the load-bearing assertion to attack."}
- framing: {stress-tester: "Framed neutral-core-first (full diff + house rules + standing motions), author claims separate NON-EXHAUSTIVE flagged 'not a fence'. Emphasized: predicate-vs-claim; guard-mutation (guard+subject+call-site); cheapest-passing-world; live-population-probe. Three MOTIVATING FAILURES: (1) DECISION tokens Task 28 parses must never lie — verify the conditional-gloss fix is COMPLETE across ALL tokens, attack with inputs the fold's own synthetic test did not use; (2) the earned-LOCATED floor guard must refuse the FULL FAMILY of wrong-body shapes (HTML, zero-row, no-modeled-CMA-rows, truncated, delimiter mismatch, missing-TOTAL) not just header-only; (3) the skip branch must not launder a code fault. b0.5 mutation-harness hazard flagged. Required separation of value-WRONG/note-asserts-false from all else."}
- not_fired: {quant-financial-engineer: "no quant-finance math (schema record, not computation). RE-TRIAGE PROMISE STANDS: Task 26.", backtest-validator: "no CV/label/walk-forward/ML/backtest-metric. Genuine no-trigger-match.", data-integrity-validator: "P5 persists NO data artifact — it records the IRCC feed's url + column schema as a CONTRACT; the actual ingestion/PIT-join/staleness surface lands where the tripwire READS the feed live. RE-TRIAGE PROMISE: dispatch at Task 28 (PR-landings tripwire ingests the IRCC CSV) — in addition to standing Task 13/15/15b promises. DEBT, honor at Task 28."}
- bypass: null
- outcome: DISPATCH TERMINATED EARLY by a session/API limit (~20 tool uses). Per the DEEP-dispatch-death protocol, the incremental audit file (/tmp scratchpad p5-stress-audit.md, mandated incremental-write) was READ FIRST and carried a COMPLETE verdict: REOPENER C1 [HIGH: DECISION-CSV-XLSX-UNROUNDED-URL asserts the XLSX is "unrounded, for when base-5 rounding matters" — live-verified FALSE: XLSX scanned 30,312 cells all multiples-of-5 + 5,385 `--` suppressed, identical rounding+suppression to the CSV; controller independently confirmed `--` present in the XLSX], plus carries C2 [category gloss self-contradicts if cat_url==''] and C3 [_col_index substring-matches SUBTOTAL]. C1 + C2 FOLDED (final P5 fold); C3 carried. Verified-clean by the pass (recovered): guard catches all non-tabular wrong bodies, skip branch cannot launder a code fault, provenance count matches, FOUND-AT-CMA token+gloss consistent. Re-dispatch was NOT needed — the incremental file delivered the full verdict.

### 2026-07-28 · task-scoped · ruling-E-wds-extraction (f0b6736 + 896d5c0 + 80a95af)
- tier: STANDARD
- changed_shape: money-path NO (repo AGENTS.md `money-path: no`); load-bearing-claim YES — matched "the working set stays working"/byte-preservation, "a code fault must never launder into pytest.skip", a fail-loud registry (`Fact.__post_init__` RAISES with no active run), "the floor guard is load-bearing", and "the reachability probe must use the SAME HTTP method as the real call". File-glob classes hit = refactor of live vendor-boundary probe machinery (StatCan WDS / CKAN / IRCC) whose outputs are COMMITTED artifacts, plus new guard/invariant code (18 then 32 test gates) and a new pinned fixture. No math substrate, no CV/label/walk-forward, no ArcticDB/broker write.
- candidate_reviewers: [quant-financial-engineer, stress-tester, backtest-validator, data-integrity-validator]
- fired: [stress-tester]
- fired_why: {stress-tester: "trigger-matrix row 'New/modified hardening code, regression tests, or invariant checks' AND the #437 load-bearing-claim arm, which fires REGARDLESS of money-path — here the claims ARE the deliverable (byte-preservation of committed artifacts, a fail-loud registry, and gates asserted load-bearing)."}
- tier_note: path-derived tier was NARROW (non-money-path; tests + glue). UP-TIERED to STANDARD read depth by the controller: NARROW passes diff-only, which structurally cannot see whether the rewire changed the three probes' BEHAVIOR — the entire claim under review. Recorded so the up-tier is auditable, not silent.
- framing: {stress-tester: "Task-scoped, STANDARD tier, neutral-core-FIRST with the author's claims in a SEPARATE section explicitly labelled 'an anchored, NON-EXHAUSTIVE set; siblings may be un-named' and instructed to be read only AFTER forming a view from the diff. Emphasized motions inlined verbatim (reviewer never sees the skill body): guard-mutation — spelled out in full, mutate the guard AND (a) the SUBJECT it protects AND (b0) the CALL SITE, since guard-body mutation alone proves only that it is WIRED; predicate-vs-claim — attack each predicate as a PROXY, construct an input where the property is VIOLATED but the predicate SATISFIED, with three in-family anchors (non-strict <=, row-COUNT proxy, .empty short-circuit); test-double-divergence — the offline tests patch mod._search/_fetch_csv/_catalogue/_meta_batch/_guard_csv_shape/mod.OUT, so ask where a green offline test vouches for a broken live path and specifically whether the extraction moved anything ACROSS a seam. House rules stated as the neutral core: (#1) a generated artifact may not state anything not tied to its computed state, with the four historical shapes named; (#2) a verification gate must REFUSE when it cannot verify, never false-green; (#3) a code fault must never launder into pytest.skip, skip only on a network-class EXCEPTION TYPE + same-HTTP-METHOD reachability. REOPEN CRITERION stated BEFORE dispatch (terminal state): reopens ONLY if (a) the rewire changed observable behavior — note bytes, wire behavior, or a verdict — or (b) a gate CLAIMED load-bearing is proven VACUOUS under mutation; everything else routes to a CARRY list, with the reviewer asked to label each finding itself and to return the TRUE split rather than inflating or suppressing. Incremental audit-file write mandated (docs/audits/2026-07-28-t1-wds-extraction-stress.md) so a dispatch death keeps partial work. b0.5 mutation-harness hazard flagged as SAFE here because the work is committed, but restoration ordered verified by cmp/git-status rather than assumed. Told it holds the tree ALONE (mutating reviewer) and that .lane-checkpoints.md is the controller's — not input, not to be modified. Closing weight given: 'two probes (P5b, P6) will be written against these shared modules NEXT by an author who will copy an existing probe's call block — a trap a copy-paste author would fall into is worth more here than an abstract one.'"}
- not_fired: {quant-financial-engineer: "This diff contains NO arithmetic at all — it is a mechanical extraction of HTTP/provenance machinery plus test gates. No variance/vol/Greeks/units-conversion/sizing/ranking/PIT-join. Genuine no-trigger-match for this diff. RE-TRIAGE PROMISE STANDS UNCHANGED from the task-2/3/4 records: dispatch at Task 26 (OwnerStock defining equation + excess-demand fraction), the first production math.", backtest-validator: "no CV splitter, no label construction, no walk-forward, no train/test split, no ML pipeline, no reported backtest metric. Genuine no-trigger-match; nothing in Tranche-1 tasks 0-30 introduces one.", data-integrity-validator: "This task commits NO data artifact and adds NO data source — it moves existing machinery between modules and pins its behavior. The committed P*.md notes were PROVEN byte-unchanged (record/replay + offline + paired-live), so there is no new schema, write target, staleness surface or PIT join to attack. RE-TRIAGE PROMISES STAND, unchanged and NOT re-deferred by this record: Task 13 (ownership loader, first CONSUMER read of the pinned P2 extract), Task 15 (multiplier constant lands committed), Task 15b (living_arrangement.json committed + loaded), and Task 28 (first live ingestion of the IRCC URL). These are DEBTS; honor at the named task."}
- bypass: null
- verdict: PROCEED-WITH-MODIFICATIONS. 31 mutants: 22 KILLED, 9 SURVIVED. **0 REOPEN, 8 CARRY** — the stated criterion was respected, not inflated. Limb (a) proven by a RECORD/REPLAY harness the reviewer built (monkeypatch urllib.request.urlopen, tape every response keyed on (url, method, body) from the NEW code against the live source, then replay the SAME tape through the pre-extraction module from `git show e043500:`) — this removes data-vintage drift as a confounder, which two separate live runs cannot. Result: p3/p4/p5 all BYTE-IDENTICAL (22187/16587/9039 chars, 0 tape misses) with wire sequences identical in url/method/headers/body/timeout and order. That covers p3, which NOTHING in the suite executes. Limb (b) clean: every gate mutated against its own named claim was KILLED, including the combined free-variable-header + forgotten-new_run() mutation. FOLDED (80a95af): C1 [HIGH, listed by nobody — a hand-typed provenance-header literal SURVIVED in run_p5.py:585 AND run_p3.py:810, so house rule #1 had NO call-site enforcement in ANY probe; the anti-contamination gate compares a COUNT LINE for order-independence and a constant is perfectly order-independent, so no behavioural gate could ever have caught it]; C2 [HIGH — the reachability gate ENUMERATED wrappers by name so a new probe was uncovered, and restoring `method: str = "GET"` SURVIVED; live-verified that p5's GET+Range shape against WDS_META returns False, so a P6 copying p5's wrapper would report a HEALTHY service unreachable and launder every failure into pytest.skip]; C3 [MED, the same count-proxy just fixed for NETWORK_EXCEPTIONS left in the other spelling]; C4 [MED, "provenance_header reads only the log it is handed" is the SOLE justification for the accepted `_ACTIVE` residual and was itself unpinned]; C5 [MED, `Fact` did not validate `kind` — a typo'd kind yields `registered 3: 1 DERIVED and 1 CITED`, a FALSE COUNT in a generated artifact]. Controller design ruling on the fold: the gates DISCOVER probes by GLOB rather than enumerating them by name, so P5b/P6 are covered ON ARRIVAL instead of depending on a future author extending a list; C1 uses a STATIC AST gate so it covers p3 offline. Acceptance test = a throwaway p6 cloning p5's call block failed THREE glob-discovered gates with ZERO registration; controller independently reproduced the attribution gate firing. CARRIED (not folded): C6 [p3's main() executed by nothing — structural, pre-existing], C7 [fail-vs-skip is dead code in CI: no committed note records a failure, so `grep -c "LIVE PROBE FAILED"` is 0/0/0 and only the offline mutation tests exercise that path], C8 [`spec_from_file_location` + `__pycache__` validate on (mtime in whole SECONDS, size), so a same-second same-size edit silently reuses STALE bytecode — it bit the reviewer's own first pass with 7 failures on a clean tree; any future mutation harness here needs PYTHONDONTWRITEBYTECODE=1 + a cache purge]. R4 (p3/p4 multi-boundary reachability gap) remains DEFERRED by controller ruling with no TODO — it fails in the SAFE direction (a spurious FAIL, never a false-clean skip).

### 2026-07-28 · task-scoped · task-6 (probe P5b; a538571 + edf559a + 66ad580)
- tier: STANDARD
- changed_shape: money-path NO (repo AGENTS.md `money-path: no`); load-bearing-claim YES — a probe whose DECISION tokens are load-bearing claims BY CONSTRUCTION (standing interpretation pinned at the 2026-07-28 steering fold), plus an explicit floor guard asserted never-false-green, a three-valued verdict vocabulary, and a "MEASURED not assumed" claim. File-glob classes hit = live vendor-boundary probe across FIVE boundaries (StatCan getCodeSets / getAllCubesListLite / getCubeMetadata, CKAN package_search, an ISQ workbook) generating a COMMITTED artifact, plus new guard/invariant code. No math substrate, no CV/label/walk-forward, no ArcticDB/broker write, no committed DATA artifact (the findings live in a generated note).
- candidate_reviewers: [quant-financial-engineer, stress-tester, backtest-validator, data-integrity-validator]
- fired: [stress-tester]
- fired_why: {stress-tester: "trigger-matrix row 'New/modified hardening code, regression tests, or invariant checks' AND the #437 load-bearing-claim arm (fires regardless of money-path). The standing interpretation makes this MANDATORY for every remaining probe task: author-side rules are demonstrably insufficient here — P5 fabricated despite explicit instructions, and the un-anchored adversarial live probe was the only catch."}
- tier_note: path-derived tier NARROW (non-money-path; probe + tests). UP-TIERED to STANDARD read depth: NARROW passes diff-only, which cannot reach the live boundaries the verdict rests on, and this probe's whole risk surface is the relationship between a live response and the prose written beside it.
- framing: {stress-tester: "Task-scoped, STANDARD tier, MANDATORY live-boundary pass. Neutral-core-FIRST (full diff + the three house rules + standing motions), author claims in a SEPARATE section labelled 'an anchored, NON-EXHAUSTIVE set; siblings may be un-named', to be read only AFTER forming a view from the diff. Motions inlined verbatim: guard-mutation (spelled out — mutate the guard AND (a) the SUBJECT it protects AND (b0) the CALL SITE, since guard-body mutation alone proves only that it is WIRED); predicate-vs-claim (attack each predicate as a PROXY; construct an input where the property is VIOLATED but the predicate SATISFIED, with three in-family anchors); live-population-probe (the verdict rests on a SWEEP over a live catalogue — go to the real boundary; ask what the live population can contain that the predicate mishandles, and whether any absence claim is honestly SCOPED to what was swept). MOTIVATING FAILURE NAMED — today's actual incident: the prior review found the recurred defect class had moved ONE LAYER DEEPER than anyone was looking, verdict verbatim 'every NUMBER in the note traces to an expression, but four ADJECTIVES beside those numbers do not' — four glosses beside correctly-computed values were themselves hand-typed (an explanation naming a word nothing inspected; a conclusion asserting 'maintained' when maintenance was untested; a blanket 'the only claim this note makes' falsified by the note's own later section; an asserted containment relation never computed). Instructed: assume siblings survive; the number being right is exactly what stops anyone checking the sentence beside it; treat 'the value is correct' as NO evidence about the prose around it. TWO HARNESS HAZARDS handed over, both learned in-session: (i) spec_from_file_location + __pycache__ validate on (mtime in whole SECONDS, size) so a same-second same-size edit reuses STALE bytecode — run PYTHONDONTWRITEBYTECODE=1 and purge; (ii) a mutation test that does not verify the mutation APPLIED is itself a false-green — assert target in source before writing. REOPEN CRITERION stated BEFORE dispatch: reopens ONLY if (a) a recorded VALUE is WRONG or (b) the note ASSERTS SOMETHING FALSE; all else routes to a CARRY list, reviewer to label each finding itself and return the TRUE split. Incremental audit-file write mandated. Told it holds the tree ALONE and that .lane-checkpoints.md is the controller's. Closing weight: 'one more probe (P6) will be written against this same shape by an author who will copy p5b's call block — a trap a copy-paste author falls into is worth more than an abstract one.'"}
- not_fired: {quant-financial-engineer: "P5b performs NO quant math — it sweeps a catalogue, resolves cube metadata, and classifies stock-vs-flow from titles/dimensions/footnotes. No variance/vol/Greeks/units-conversion/sizing/ranking/PIT-join. Genuine no-trigger-match. RE-TRIAGE PROMISE STANDS UNCHANGED from the task-2/3/4 records: dispatch at Task 26 (OwnerStock defining equation + excess-demand fraction), the first production math.", backtest-validator: "no CV splitter, no label construction, no walk-forward, no train/test split, no ML pipeline, no reported backtest metric. Genuine no-trigger-match; nothing in Tranche-1 tasks 0-30 introduces one.", data-integrity-validator: "TRIGGER PARTIALLY MATCHED — P5b identifies a NEW SOURCE (StatCan 17-10-0121-01) and records its schema and cadence, which is DIV's home ground. NOT suppressed, and NOT dismissed: P5b commits NO data artifact and performs NO ingestion — the findings live in a generated observation note, so there is no persisted write target, no schema to drift, no staleness policy and no PIT join to attack yet. The DIV surface lands where the artifact does. RE-TRIAGE PROMISE: dispatch when this source is WIRED (the temporary-resident-stock tripwire indicator), in ADDITION to the standing Task 13, Task 15, Task 15b and Task 28 promises. These are DEBTS accumulated across five records now; honor each at its named task and do NOT re-defer."}
- bypass: null
- verdict: PROCEED-WITH-MODIFICATIONS. **2 REOPEN / 11 CARRY.** Live regen across all FIVE boundaries came back BYTE-IDENTICAL, so **zero findings under limb (a)** — every recorded value reproduces; the defects were entirely in claims ABOUT correct values. REOPEN-1 [limb b] the abbreviation check's CONCLUSION was logically INVERTED: `abbrev_pop` counted npr-hits that ALSO matched a population phrase — i.e. cubes the phrase predicate had ALREADY caught — so `abbrev_pop == 0` could not support "no cube names the population by abbreviation alone"; BOTH branches concluded the opposite of what the count meant and the parenthetical "(MEASURED not assumed)" was FALSE as committed. Proven with two offline fixtures. **The pattern is the lesson: this sat ONE CLAUSE EARLIER than the "nonprofit" gloss the previous fold had just fixed for being true-only-by-accident — same accident, survived the fix, because that fix was aimed at WORDING and this defect was in the LOGIC.** REOPEN-2 [surviving mutant on a gate claimed load-bearing] dropping `eligible` and forcing `_is_current`→True BOTH SURVIVED 60/60 while the docstring claimed the eligibility filter runs first "so the freshness ranking cannot be hijacked"; the mutated pick became "Temporary foreign workers in the agriculture sector, by category of farm revenue" still stamped LOCATED — surviving precisely BECAUSE the real pick happens to hold the latest end date, i.e. zero live blast radius was the REASON the mutant lived. Both FOLDED at 66ad580 with a discriminating fixture that inverts the protecting accident (non-eligible cube ends 2027, archived-eligible 2028, both fresher than the population cube). Also folded: C1 (the test file MIS-GRADED ITS OWN GUARD — `_guard_codesets` is attribution-only, structurally incapable of being safety-load-bearing since an empty map makes `_guard_pick`'s check always True; the file grades `_guard_sweep` honestly two lines later, so it set the standard and missed one guard over, and the audit's own risk list inherited the error — independently re-verified by the implementer rather than taken), C2 (the previous fold's fix was honest-about-provenance but UNPINNED: flipping the declared mapping to "Ontario" survived green), C3 (a THIRD surviving unenforceable blanket claim), C9 (the note asserted "the demand model's NPR input" in PRESENT tense while `demoflow/src/demoflow/` is `__init__.py` + `errors.py` — controller ruling: true at DESIGN level per spec §6, restate as the specified consumer not an implemented one), C5 (the P6 copy-paste trap: title-scoped vs title+dims+members-scoped evidence glossed symmetrically). Six mutants all RED after the fold, each verified APPLIED first; controller independently re-ran the `_is_current` mutant and confirmed RED (it had survived 60/60 pre-fold). CARRIED to the field report: C4, C6, C7, C8, C10, C11, tabulated in the audit's fold-disposition table. **Process lesson recorded: three of the fold's own mutants survived the implementer's first attempt for one shared reason — only the POSITIVE direction was tested; a check that always answers CORROBORATED never answers NOT. The fail-safe-gate rule ("the FAILURE behaviour is the teacher") had been applied to the probe's guards but not to the author's own new test assertions.**
